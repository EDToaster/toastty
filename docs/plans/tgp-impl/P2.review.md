# P2 verify (pass 1)

Verification pass over the **revised** `P2.md` (Differentiators) after the cleanup pass that applied
the reconciliation **addendum** (G11 isolation / G12 error-code reachability) and discharged the
DEDUP duty (reuse P1-T12 for instance decode/validation). This pass checks four things the brief
demands: (1) full coverage of every P2-scoped protocol surface, (2) addendum conformance (no `denied`
on cross-token collision; cross-token refs use `bad_ref`/late-bind per G11; error codes match the
G12 table), (3) the DEDUP duty is *applied, not re-implemented*, and (4) general correctness
(no vague/invented tests, acyclic backward-only deps, no G-invariant contradiction).

Inputs read in full: `P2.md` (T1-T38 + round-1/round-2/cleanup resolution logs), the addendum
(`tgp-reconciliation-addendum.md`, G11/G12), the base spine (`tgp-reconciliation.md`, G1-G10), and
design slices `s10.md`, `s12.md`, `s06.md`.

Dependency graph machine-checked: **acyclic, no missing nodes** (32 intra-P2 nodes). The only
numeric-vs-topological inversion is the documented "T36 helper built before its callers
T11/T12/T13 and the T1 `op=remove` apply" — backward-only in the build DAG, explicitly flagged in
the T36 build-order note. All deps point at lower-numbered P2 tasks, P1, or P1-T12.

## Coverage

Legend: COVERED = >=1 concrete test (cited task + test name); N/A-Pn = surface owned by another
phase (noted, not a P2 defect). UNCOVERED would be a defect — none found.

### Ops / verbs

| Surface | Status | Task + test |
|---|---|---|
| `vp` upsert (create / sparse-merge) | COVERED | P2-T1 `vp_create_two_viewports_distinct_ids`, `vp_sparse_merge_preserves_unset_fields` |
| `vp` `op=remove` (explicit destroy + unknown-id no-op) | COVERED | P2-T1 `vp_op_remove_destroys_viewport`, `vp_op_remove_unknown_id_noop`; P2-T36 `destroyed_explicit_reason` |
| `vp` `render` sub-map (tone_map / theme_tint) | COVERED | P2-T1 `vp_render_opts_decoded` |
| `node.instances` (replace whole buffer; decode owned by P1-T12, surfaced) | COVERED | P2-T16 `instances_decode_surfaced_to_node`; P2-T18 `instances_bump_scene_not_asset_revision` |
| `node.instance_set` (single re-tint, genuinely P2-owned) | COVERED | P2-T18 `instance_set_writes_one_record`, `instance_set_bad_index` |
| `node.upsert` Instanced kind (P2-wired) | COVERED | P2-T16 `instanced_node_kind_set` |
| `sub` verb decode / store | COVERED | P2-T21 `sub_decode_and_store`, `sub_explicit_ids`, `sub_ev_mask_full_decode` |
| `vp;explore` sub-map decode + controller | COVERED | P2-T28 `explore_orbit_rotates_camera_node` (+ full explore suite) |
| `anim;play` (camera_busy reject slice only; P4 owns full op) | COVERED (P2 slice) | P2-T30 `explore_blocks_anim_play_camera_busy` |
| `node.remove` interaction (vp cam/root, subs, hover) | COVERED | P2-T14 `node_remove_camera_falls_back`; P2-T21 `sub_node_remove_drops_from_list`; P2-T26 `hover_leave_on_node_removed` |
| `apc_end` back-channel decoded-frame return (cross-crate change) | COVERED | P2-T1 `vp_apc_end_returns_decoded_frame` |

### Header / frame fields this phase introduces

| Surface | Status | Task + test |
|---|---|---|
| `tok=` (private-map isolation + emit tag) | COVERED | P2-T1 `vp_same_raw_id_two_tokens_coexist`; P2-T21 `sub_cross_token_matches_nothing`; P2-T23 `event_tagged_with_token` |
| `vp=` (viewport id correlator; composite `{token,raw}`) | COVERED | P2-T1 `vp_create_two_viewports_distinct_ids`, `vp_same_raw_id_two_tokens_coexist` |
| `anchor=inline` + `line=`/`col=` | COVERED | P2-T2 `inline_create_binds_cells`, `inline_hint_not_reread` |
| `anchor=pinned` + `col=`/`row=` | COVERED | P2-T1 `vp_create_two_viewports_distinct_ids`; P2-T3 `px_mapping_basic` |
| `cols=`/`rows=` (CellRect u16) | COVERED | P2-T1 `vp_sparse_merge_preserves_unset_fields`; P2-T4 `inline_over_cap_cols_rejected` |
| `z=` | COVERED | P2-T8 `zorder_by_z_ascending` |
| `clear=` (Some / None / null-reset) | COVERED | P2-T1 `vp_clear_null_resets_to_transparent`; P2-T7 `composite_clear_some_occludes` |
| `screen_affinity=` (primary/alt/both) | COVERED | P2-T15 `alt_screen_both_persists` |
| `clip_to_scroll_region=` | COVERED | P2-T10 `clip_to_scroll_region_true_clips_at_margin`, `clip_to_scroll_region_false_clips_to_bounds` |
| `camera=` (vp late-bound + null clear) | COVERED | P2-T14 `vp_camera_before_node_then_adopts`; P2-T1 `vp_camera_null_clears_to_default` |
| `root=` (vp late-bound + null clear) | COVERED | P2-T14 `vp_no_root_uses_whole_scene`; P2-T1 `vp_root_null_clears_to_whole_scene` |
| `explore=` (vp sub-map, nullable tear-down) | COVERED | P2-T28 `explore_orbit_rotates_camera_node`, `explore_without_feat_unsupported` |
| `count=` (instances; P1-T12 decode surfaced) | COVERED | P2-T16 `instances_decode_surfaced_to_node`, `instances_count_zero_empty_surfaced` |
| `xforms` (CBOR bytestring, mat4 LE col-major; surfaced) | COVERED | P2-T16 `instances_decode_surfaced_to_node`, `instances_translation_value_surfaced` |
| `tints` (CBOR bytestring RGBA8 sRGB; surfaced) | COVERED | P2-T16 `instances_tints_surfaced`, `instances_tints_absent_surfaced_none`, `instances_tint_srgb_to_linear` |
| `index=` (instance_set) | COVERED | P2-T18 `instance_set_writes_one_record`, `instance_set_bad_index` |
| `nodes=*` / `nodes=<ids>` (sub target) | COVERED | P2-T21 `sub_decode_and_store`, `sub_explicit_ids` |
| `ev=` mask (sub) — whole closed set + ignore-unknown | COVERED | P2-T21 `sub_ev_mask_full_decode`, `sub_ev_garbage_token_ignored` |
| `raw=` (sub) | COVERED | P2-T22 `sub_raw_alone_ok`, `sub_raw_plus_events_rejected` |
| `both=` (sub, F4 dual-emit) | COVERED | P2-T33 `both_flag_emits_both` |
| explore opts (orbit/zoom/pan/lock_axes/zoom_min/max/auto_spin/damping/initial) | COVERED | P2-T28 `explore_orbit_*`, `explore_zoom_clamped`, `explore_pan_translates_camera_node`, `explore_lock_axes_turntable`, `explore_auto_spin`, `explore_damping_decays_to_rest`, `explore_initial_pose_seeds_camera` |
| `node=` / `inst=` (event) | COVERED | P2-T23 `event_click_wire_format` |
| `btn=` (event, pointer-button only) | COVERED | P2-T23 `event_btn_serialized` |
| `x=`/`y=` normalized coords (event) | COVERED | P2-T23 `event_normalized_coords` |
| `wx=`/`wy=`/`wz=` world hit point (event, verbatim float) | COVERED | P2-T23 `event_world_point_in_frame` |
| `rev=` (event scene_revision stamp) | COVERED | P2-T20 `pick_rev_stamp`; P2-T23 `event_click_wire_format` |
| `t=`/`r=`/`s=` (ev=camera sync-back, round-trip-identical) | COVERED | P2-T31 `camera_syncback_on_drag_end`, `camera_syncback_roundtrip_identical` |
| `cols=`/`rows=` (ev=resize) | COVERED | P2-T35 `resize_event_emitted` |
| `reason=` (ev=destroyed: evicted/detached/explicit) | COVERED | P2-T36 `destroyed_evicted_routed_to_owner`, `destroyed_detached_reason`, `destroyed_explicit_reason` |
| `v=`/`vmin=` (unsolicited tgp;r on device-loss) | COVERED | P2-T37 `device_loss_pushes_tgp_r` |
| `detail=` (advisory on x) | COVERED | P2-T4 (`detail=max_inline_cols`); P2-T17 (multiple); P2-T16/T22/T28 (`detail=instance`/`pick`/`explore`) |

### Error codes (G3 closed enum) — checked against the G12.3 reachability table

| Code | G12.3 status | P2 status | Task + test (or note) |
|---|---|---|---|
| `cap_exceeded` | REACHABLE | COVERED | P2-T4 `inline_over_cap_cols_rejected`/`inline_over_cap_rows_rejected`; P2-T5 `viewport_px_over_cap_rejected`; P2-T17 `instances_over_cap_rejected_retains_prior` (`detail=max_instances`) |
| `bad_layout` | REACHABLE | COVERED | P2-T17 `instances_bad_layout_retains_prior_xforms_len`, `instances_bad_layout_tints_len`, `instances_count_mismatch_detail` (validation owned by P1-T12, surfaced via apply path) |
| `parse_error` (`detail=instance_nan`) | REACHABLE | COVERED | P2-T17 `instances_nan_rejected_retains_prior` (P1-T12 validator, P2 retention) |
| `kind_conflict` (`detail=instanced_node_tint`) | REACHABLE (row 11) | COVERED | P2-T17 `instanced_node_level_tint_rejected` — G12.4-correct (`kind_conflict`, NOT `bad_param`) |
| `bad_index` | REACHABLE | COVERED | P2-T18 `instance_set_bad_index` |
| `bad_ref` (vp camera/root missing, optional diag, no oracle) | REACHABLE | COVERED | P2-T14 `vp_camera_missing_optional_bad_ref`; P2-T1 `vp_cross_token_camera_ref_late_binds_default` (byte-identical missing-state error) |
| `unsupported` (use-time feat gate) | REACHABLE (row 18) | COVERED | P2-T16 `instances_without_feat_unsupported` (`detail=instance`); P2-T22 `sub_pick_without_feat_unsupported` (`detail=pick`); P2-T28 `explore_without_feat_unsupported` (`detail=explore`) |
| `invalid_sub` (raw/explore/wheel arbitration) | REACHABLE | COVERED | P2-T22 `sub_raw_plus_events_rejected`, `sub_raw_plus_explore_rejected`, `wheel_plus_explore_zoom_conflict` |
| `camera_busy` (explore↔anim controller) | REACHABLE | COVERED | P2-T30 `explore_blocks_anim_play_camera_busy`, `anim_playing_camera_blocks_explore` |
| `null_not_allowed` (vp non-nullable field) | REACHABLE | COVERED | P2-T1 `vp_null_on_required_field_rejected` (`detail=anchor`) |
| `bad_token` (post-RIS invalidated token frame) | REACHABLE (G11(d)) | COVERED | P2-T38 `ris_invalid_token_frame_rejected` |
| `denied` | RESERVED (G11(a)/G12.2) | CORRECTLY NOT PRODUCED | No reachable P2 trigger; closed-set round-trip is P1-T16/P5-T1. P2 body asserts *no* `denied` on cross-token (T1/T8/T21/T38). |
| `msg_too_large` | RESERVED (G12.1) | N/A-P1 | Subsumed by `cap_exceeded;detail=max_msg` at P1; P2 grep-clean. |
| `unknown_op` | RESERVED | N/A-P1 | Unknown ops skipped, not fatal (s07.3). |
| `parse_error`/`truncated` (framer) | REACHABLE | N/A-P1 | Framer/CBOR decode is P1. |
| `bad_ref`/`dup_id` (patch ref / asset add) | REACHABLE | N/A-P1 | Patch ref resolution + asset add is P1. |
| `unknown_node`/`cycle`/`depth_exceeded`/`chunk_conflict`/`vram_exhausted` | REACHABLE | N/A-P1 | Node lookup, commit graph validation, chunking, VRAM accounting are P1. |
| `unknown_clip`/`node_busy`/`not_playing`/`no_targets` | REACHABLE | N/A-P4 | Animation playback. |
| `bad_param` | REACHABLE (row 16) | N/A-P1 | Out-of-range field / node-id charset; NO LONGER carries `instanced_node_tint` (G12.4 struck). |

All 26 closed-enum members accounted for: 11 concretely produced + tested by P2 surfaces, `denied`
correctly RESERVED (not produced), the remainder owned by P1/P4. The G12.3 reachability partition is
respected exactly — every code P2 emits is REACHABLE in the table, and the 3 RESERVED codes
(`denied`, `msg_too_large`, `unknown_op`) are emitted nowhere in P2 (grep-clean confirmed).

### Capability / feat tokens consumed at P2 use-sites

| Surface | Status | Task + test |
|---|---|---|
| `feat=instance` use-time gate | COVERED | P2-T16 `instances_without_feat_unsupported` |
| `feat=pick` (⇒event) use-time gate | COVERED | P2-T22 `sub_pick_without_feat_unsupported` |
| `feat=explore` (⇒event) use-time gate | COVERED | P2-T28 `explore_without_feat_unsupported` |
| `max_inline_cols` / `max_inline_rows` | COVERED | P2-T4 `inline_over_cap_*`, `inline_at_cap_accepted`, `inline_update_over_cap_keeps_prior` |
| `max_viewport_px_w` / `max_viewport_px_h` (family `max_viewport_px`) | COVERED | P2-T5 `viewport_px_over_cap_rejected`, `viewport_px_clamped_under_cap_accepted` |
| `max_instances_per_buffer` | COVERED | P2-T17 `instances_over_cap_rejected_retains_prior` |
| `click_deadzone_px` | COVERED | P2-T32 `click_no_motion_emits_click`, `two_clicks_outside_deadzone_no_dblclick` |
| `camera_report_hz` | COVERED | P2-T31 `camera_syncback_throttled_by_hz` |
| `max_backchannel_bytes` | COVERED | P2-T34 `backpressure_drops_oldest_coalescable` |
| `color=srgb` (wire → linear → sRGB offscreen) | COVERED | P2-T7 `composite_premul_over_srgb_value`, `composite_clear_some_color_is_opaque_fill`; P2-T16 `instances_tint_srgb_to_linear` |
| dblclick threshold (fixed constant per §12.5, NOT a v1 cap) | COVERED | P2-T32 `dblclick_within_threshold_emits` |
| `max_lights_per_viewport` | N/A-lighting | Not exercised by P2 surfaces. |
| `anim_speed_max` | N/A-P4 | Animation. |

### Event types — closed `ev` enum (§12.5)

| ev | Status | Task + test |
|---|---|---|
| `click` | COVERED | P2-T23 `event_click_wire_format`; P2-T32 `click_no_motion_emits_click` |
| `dblclick` | COVERED | P2-T32 `dblclick_within_threshold_emits`, `two_clicks_past_threshold_no_dblclick`, `two_clicks_outside_deadzone_no_dblclick` |
| `down` | COVERED | P2-T32 `down_up_emitted_on_press_release` |
| `up` | COVERED | P2-T32 `down_up_emitted_on_press_release`; P2-T27 `focus_out_synthesizes_up_then_leave` |
| `hover` | COVERED | P2-T26 (hover machine); P2-T34 `coalesce_consecutive_hover` |
| `enter` | COVERED | P2-T26 `hover_enter_on_move_onto_node`, `hover_leave_then_enter_on_retarget` |
| `leave` | COVERED | P2-T26 `hover_leave_on_node_removed`, `hover_leave_on_geometry_moves_away`; P2-T27 |
| `drag` | COVERED | P2-T32 `drag_forwarded_when_explore_disabled` |
| `wheel` | COVERED | P2-T32 `wheel_event_sub_owns` |
| `camera` | COVERED | P2-T31 `camera_syncback_on_drag_end` |
| `resize` | COVERED | P2-T35 `resize_event_emitted` |
| `destroyed` | COVERED | P2-T36 `destroyed_evicted_routed_to_owner` |

### Lifecycle events (G10)

| Surface | Status | Task + test |
|---|---|---|
| inline detach-on-overwrite (revert cell) | COVERED | P2-T11 `overwrite_detaches_cell`, `reemit_identical_glyph_no_detach` |
| detach-below-count auto-destroy (reason=detached) | COVERED | P2-T11 `detach_below_count_destroys_silent`, `detach_below_count_calls_helper_with_detached`; P2-T36 `destroyed_detached_reason` |
| scrollback eviction (reason=evicted) | COVERED | P2-T12 `eviction_destroys_inline_silent`, `eviction_calls_helper_with_evicted`, `partial_eviction_keeps_alive` |
| ED/EL/2J erase inline destroy; pinned persists | COVERED | P2-T13 `csi_2j_destroys_inline`, `csi_2j_keeps_pinned`, `partial_el_keeps_inline`, `csi_2j_keeps_scene_nodes` |
| explicit destroy (vp op=remove, reason=explicit) | COVERED | P2-T1 `vp_op_remove_destroys_viewport`; P2-T36 `destroyed_explicit_reason` |
| shared destroy helper (free targets/subs/pick) | COVERED | P2-T36 `destroyed_frees_subs_and_pick_target`, `destroyed_silent_without_sub`, `destroyed_never_coalesced` |
| alt-screen affinity hide/restore (no teardown) | COVERED | P2-T15 `alt_screen_hides_primary_viewport`, `alt_screen_both_persists`, `alt_screen_exit_restores`, `hidden_inline_not_evicted` |
| reflow (SIGWINCH/font/rewrap) recompute from cells | COVERED | P2-T9 `reflow_follows_cells_on_resize`, `reflow_sets_only_viewport_dirty`, `reflow_split_bounding_rect` |
| DECSTBM scroll moves inline | COVERED | P2-T10 `decstbm_scroll_moves_inline_rect`, `inline_not_downgraded_in_region` |
| device-loss rebuild + force re-upload (ignore dirty) | COVERED | P2-T37 `device_loss_realloc_targets_ignores_dirty`, `device_loss_reuploads_instances_ignores_instance_dirty`, `device_loss_retains_cpu_scene_state` |
| device-loss unsolicited tgp;r push | COVERED | P2-T37 `device_loss_pushes_tgp_r` |
| RIS full teardown across tokens + invalidate (silent) | COVERED | P2-T38 `ris_destroys_all_viewports_and_targets`, `ris_cancels_subs_explore_and_invalidates_tokens`, `ris_destroy_is_silent_no_destroyed_event` |
| post-RIS invalid-token frame rejected (bad_token, G11(d)) | COVERED | P2-T38 `ris_invalid_token_frame_rejected` |
| DECSTR soft-reset leaves scene intact | COVERED | P2-T38 `decstr_leaves_viewports_intact` |
| foreground-change / PTY-close emission gating | COVERED | P2-T35 `foreground_change_cancels_and_stops`, `pty_close_cancels_token` |
| focus-out gesture termination (up+leave, damping) | COVERED | P2-T27 `focus_out_synthesizes_up_then_leave`, `focus_out_ends_explore_drag_damps`, `focus_in_no_auto_resume` |
| sub lifecycle (remove drops, recreate no-revive, vp-destroy drops) | COVERED | P2-T21 `sub_node_remove_drops_from_list`, `sub_recreate_does_not_revive`, `sub_viewport_destroy_drops_subs` |
| explore materialize terminal-owned camera | COVERED | P2-T29 `explore_materializes_camera_when_none`, `no_explore_no_materialized_camera`, `explore_uses_existing_camera_no_materialize` |

### Compositing / layout / picking internal CPU seams (G6)

| Surface | Status | Task + test |
|---|---|---|
| cell→pixel + on-screen clamp + zero-size + offscreen | COVERED | P2-T3 `px_mapping_basic`, `px_clamp_to_screen`, `px_zero_size_not_rendered`, `px_fully_offscreen_none` |
| offscreen color+depth alloc/free gated by is_rendered | COVERED | P2-T6 `target_allocated_for_rendered_viewport`, `target_not_allocated_for_zero_size`, `target_realloc_on_resize_dirty`, `target_freed_when_offscreen` |
| depth-aware composite decision enum | COVERED | P2-T7 `composite_opaque_in_front_covers_text`, `composite_opaque_behind_text_hidden`, `composite_alpha0_shows_text`, `composite_clear_some_occludes` |
| composite color value (premul-over + sRGB round-trip) | COVERED | P2-T7 `composite_premul_over_srgb_value`, `composite_clear_some_color_is_opaque_fill` |
| z-order total ordering + cross-token tiebreak | COVERED | P2-T8 `zorder_by_z_ascending`, `zorder_tiebreak_by_token_then_id`, `zorder_inline_and_pinned_share_space` |
| instance buffer surfaced (count/xforms/tints/empty; P1-T12 decode) | COVERED | P2-T16 `instances_decode_surfaced_to_node`, `instances_count_zero_empty_surfaced` |
| instance apply: atomic retention + kind-invariant (asserts P1-T12 validation) | COVERED | P2-T17 (all) |
| instance_dirty flag (not asset_revision) | COVERED | P2-T18 `instance_set_sets_instance_dirty`, `instances_bump_scene_not_asset_revision`, `instances_reupload_does_not_touch_asset_revision` |
| GPU upload mirror (count/stride/draw-range/grow/empty) — P2-owned render path | COVERED | P2-T19 `instance_upload_count_matches`, `instance_upload_clears_dirty`, `instance_upload_stride_is_layout`, `instance_buffer_grows_in_place`, `instance_draw_range_empty_for_zero_count` |
| CPU pick (AABB + instance + world point + rev) | COVERED | P2-T20 `pick_hits_node_aabb`, `pick_misses_empty`, `pick_instance_index`, `pick_world_point`, `pick_rev_stamp` |
| CPU↔CPU pick parity (ray-cast vs CPU-rasterized; GPU-free) | COVERED | P2-T20 `pick_raycast_vs_cpu_rasterized_agree` |
| effective camera/root late-bind + default | COVERED | P2-T14 `vp_no_camera_uses_default`, `vp_camera_before_node_then_adopts`, `vp_no_root_uses_whole_scene` |
| pointer/focus injection seam (non-Parser::advance) | COVERED | P2-T24 `input_pointer_does_not_mutate_scene`, `input_focus_toggle`, `input_api_routes_to_router` |
| cross-viewport hit-test (top-down z, fall-through, opaque consume) | COVERED | P2-T25 `hittest_topmost_subscribed_owns`, `hittest_falls_through_transparent_empty`, `hittest_opaque_empty_consumes`, `hittest_ignores_unsubscribed` |
| wheel ownership resolution | COVERED | P2-T32 `wheel_explore_zoom_owns`, `wheel_event_sub_owns`, `wheel_passthrough_default` |
| raw pointer forwarding | COVERED | P2-T32 `raw_consumes_pointer_stream`; P2-T33 `raw_rect_exclusive_suppresses_sgr` |
| F4 SGR suppress/route/both/reflow-follow | COVERED | P2-T33 `sgr_suppressed_inside_subscribed_rect`, `sgr_untouched_outside_rect`, `both_flag_emits_both`, `capture_follows_reflow`, `raw_rect_exclusive_suppresses_sgr` |
| back-channel FIFO order/coalesce/backpressure | COVERED | P2-T34 `coalesce_consecutive_hover`, `click_never_coalesced`, `ack_never_reordered_before_pending_event`, `backpressure_drops_oldest_coalescable`, `da1_after_tgp_r_ordering` |
| placeholder-cell binding accessor (`U+10EEEE` + ViewportId) | COVERED | P2-T2 `inline_create_binds_cells`, `inline_reemit_same_glyph_not_detach` |
| device generation accessor (rebuild marker) | COVERED | P2-T37 `device_loss_realloc_targets_ignores_dirty` |

**Coverage result:** every P2-scoped op/verb, header field, error code (11 produced + RESERVED `denied`
correctly absent + the rest N/A-P1/P4), feat use-site, cap, `ev` type, lifecycle event, and CPU seam
has >=1 concrete cited test. **Zero UNCOVERED surfaces.**

## Addendum & dedup conformance

### (2a) No test asserts `denied` on a cross-token collision — CONFORMS

Grep-verified: the only `code=denied` occurrences in the P2 task body are **negative** assertions
("**no** `tgp;x;code=denied` is queued"):

- P2-T1 `vp_same_raw_id_two_tokens_coexist` — `tok=AP2;vp=1` after AP1 created `vp=1` creates a
  distinct `{AP2,1}` (cols=99), `{AP1,1}` unchanged (cols=10), and asserts **no `denied` of any kind**.
  This replaces the round-3 unsound `vp_namespace_isolation_denied`. It is now consistent with P2-T8
  `zorder_tiebreak_by_token_then_id` (the two raw-`vp=1` entries coexist and are merely *ordered*).
- P2-T21 `sub_cross_token_matches_nothing` — AP2's `sub vp=1;nodes=O1` resolves to `{AP2,1}` in AP2's
  own map, is **accepted** (accept-nonexistent rule §12.5.1), **matches nothing**, and asserts **no
  `denied`**. Replaces the round-3 unsound `sub_token_isolation`.
- P2-T38 deliverable explicitly states the post-RIS invalidated-token frame draws `bad_token`, **not**
  `denied` ("`denied` is RESERVED under G11(a) and would never apply here").

No P2 test asserts a *positive* `denied`. This is exactly correct under G11(a)/G12.2 (`denied`
RESERVED, no reachable ingress trigger). The two round-3 HIGH issues (P2R3-7/P2R3-8) are resolved.

### (2b) Cross-token refs use bad_ref / late-bind per G11 — CONFORMS

- The only ref-like fields P2 introduces are viewport `camera`/`root`, which are **not** patch refs
  (G2/G11(c)). P2-T1 `vp_cross_token_camera_ref_late_binds_default` asserts AP2 naming AP1's `cam`
  late-binds to `ResolvedCamera::Default` (never `denied`, never reject), with the **no-existence-oracle**
  invariant explicitly pinned (G11(b)): the optional diagnostic `bad_ref` on entering the missing state
  is byte-identical whether `cam` exists under another token or nowhere. This matches G12.3 row 6.
- P2 introduces **no patch-ref ops** (mesh/asset/material/parent ref resolution + the `bad_ref` for those
  is a P1 surface, §6/§8.6). The cleanup log correctly marks that surface N/A here.
- A `sub` naming a cross-token node id is causally blind and matches nothing
  (`sub_cross_token_matches_nothing`), never `denied` — consistent with G11(b)'s "every resolver that
  consults the bearing token's map is causally blind to other tokens' contents."

### (2c) Error codes match the G12 table — CONFORMS

- **G12.4** (`instanced_node_tint → kind_conflict`, NOT `bad_param`): P2-T17 deliverable, the DEDUP note,
  design-refs, and `instanced_node_level_tint_rejected` all assert `code=kind_conflict;detail=
  instanced_node_tint`. The round-1 log entry I-7 (which asserted `bad_param`) is explicitly noted as a
  frozen historical record superseded by the cleanup pass per G12.4. The live body carries **no**
  `bad_param` emission for this trigger. Matches G12.3 row 11 + the G12.4 ruling exactly.
- **G12.1** (`msg_too_large → cap_exceeded;detail=max_msg`): not actionable in P2 (framer surface owned
  by P1); P2 is grep-clean of `msg_too_large` and `max_msg`. The cleanup log declines this with correct
  justification.
- **G12.2** (`msg_too_large`/`unknown_op`/`denied` RESERVED): P2 emits none of the three. The closed-set
  round-trip (all 26 codes) is owned by P1-T16/P5-T1, correctly NOT a P2 obligation.
- Every reachable code P2 produces (`cap_exceeded`, `bad_layout`, `parse_error`, `kind_conflict`,
  `bad_index`, `bad_ref`, `unsupported`, `invalid_sub`, `camera_busy`, `null_not_allowed`, `bad_token`)
  appears in the G12.3 table as REACHABLE with the matching canonical trigger.

### (3) DEDUP duty — APPLIED, not re-implemented — CONFORMS

The brief's DEDUP requirement (P2 must REUSE P1-T12 for `node.instances` decode+validation; not
introduce a new `instance.rs` decoder; own ONLY the GPU render path T19 + re-tint T18) is satisfied:

- **No second decoder.** Grep confirms every `decode_instances` mention in the P2 body attributes
  ownership to **P1-T12** ("owned by P1-T12", "validation delegated to P1-T12's `decode_instances`"),
  and the explicit line "**P2 does NOT introduce a second `instance.rs` decoder.**" T16/T17/T18 all
  point at `scene.rs` (apply/kind wiring), not a new `instance.rs`. The `decode_tint_linear` seam is
  explicitly labeled a **render color-chain helper** (consumes the P1-decoded RGBA8), not a decoder.
- **T16 recast as thin consumer + use-site policy.** Heading "(REUSES P1-T12 decode)"; authoritative
  DEDUP note; decode-value tests renamed to surface-through-accessor form
  (`instances_decode_surfaced_to_node`, `instances_translation_value_surfaced`, `instances_tints_surfaced`,
  `instances_tints_absent_surfaced_none`, `instances_count_zero_empty_surfaced`) asserting through
  `Term::tgp_node_instances` that P2 surfaces the P1-decoded buffer unaltered. T16 owns only the
  `NodeKind::Instanced` wiring + the P2-only `feat=instance` use-time gate.
- **T17 recast as scene-apply consequences.** Validation logic delegated to P1-T12; T17 owns only the
  atomic last-committed-buffer retention on reject + the Instanced-kind tint invariant. Its tests drive
  the P1-T12 validator through the P2 apply path and add explicit "prior buffer retained" assertions
  (`instances_bad_layout_retains_prior_xforms_len`, `instances_nan_rejected_retains_prior`, etc.).
- **P2 owns ONLY render + re-tint.** T19 (GPU instancing render path) and T18 (`node.instance_set`
  re-tint) are labeled the genuinely P2-owned write/render paths, both operating **on** the P1-T12-decoded
  buffer (no re-decode). Their `Depends on` lines cite P1-T12 as the buffer/decode owner.

The duty is *applied* (recast deliverables + renamed tests + DEDUP notes), not *re-implemented*
(no new decoder/validator deliverable text remains). CONFORMS.

## Issues

No new HIGH or MEDIUM defects found in this pass. All eight prior-round (P2R3-1..P2R3-8) issues are
verified resolved in the live task body. One LOW documentation-hygiene observation, non-blocking.

### Resolved (verified landed in deliverable + concrete test)

- **P2R3-1** (HIGH, was: `explore` field dropped from `VpOp`) — RESOLVED. T1 deliverable now carries
  `explore: Option<ExploreOpts>` in the `VpOp`/`Viewport` struct, classified in the three-way merge table
  (nullable: CBOR null tears down controller; absent → preserve; value → set subject to T28 feat gate),
  with `decode_vp_frame` delegating the sub-map to T28's decoder. Decode home exists.
- **P2R3-2** (HIGH, was: T28 omits T1 dep) — RESOLVED. T28 "Depends on" now lists P2-T1 with an
  acyclicity note; deliverable states T28 *extends* T1's `decode_vp_frame`/`apply_tgp_vp`. The
  `explore_without_feat_unsupported` "non-explore fields still applied" assertion is now buildable.
- **P2R3-3** (HIGH, was: T22 omits T28 dep) — RESOLVED. T22 "Depends on" now lists P2-T28 with an
  acyclicity check (T28 → {T1,T24,T14}; no cycle). The arbitration tests requiring explore are buildable.
- **P2R3-4** (MEDIUM, was: `pick_cpu_gpu_agree` needs a GPU) — RESOLVED. Renamed to
  `pick_raycast_vs_cpu_rasterized_agree`; pinned to the §12.4 CPU↔CPU option (ray-cast vs CPU-rasterized
  buffer, both GPU-free); deliverable + design-refs updated; true GPU parity noted as a separately-gated
  integration test. GPU-free contract (G6) honored.
- **P2R3-5** (LOW, was: unnamed grid-move helper) — RESOLVED. T2 names concrete seams:
  `Term::test_scroll_grid(lines)`, `Term::set_grid_size(cols,rows)` (SIGWINCH/resize), and the DECSTBM
  byte form. T9/T10 reference them by name.
- **P2R3-6** (LOW, was: missing P1 cap/token-order deps) — RESOLVED. Added "P1 (handshake token order)"
  to T8, "P1 (caps: `camera_report_hz`)" to T31, "P1 (caps: `click_deadzone_px`)" to T32.
- **P2R3-7** (HIGH, was: `vp_namespace_isolation_denied` unsound + contradicts T8) — RESOLVED. Rewritten
  as `vp_same_raw_id_two_tokens_coexist` (coexistence, no `denied`) + `vp_cross_token_camera_ref_late_binds_default`
  (late-bind default, no `denied`, no-existence-oracle). Now consistent with T8's tiebreak. Discharges the
  G11 `vp` isolation duty.
- **P2R3-8** (HIGH, was: `sub_token_isolation` same composite-key flaw) — RESOLVED. Rewritten as
  `sub_cross_token_matches_nothing` (accepted, matches nothing, no `denied`). Discharges the G11 `sub`
  isolation duty.

### Observation (LOW, non-blocking)

- id: P2-OBS-1
- severity: low (documentation hygiene, not a plan defect)
- problem: The **frozen historical** resolution logs retain superseded test names/claims: round-1 I-7
  (line 600) still reads "assert `code=bad_param`" and round-2 P2R2-10 (line 623) references the old test
  name `instances_count_zero_empty_ok`. These are explicitly historical records — the cleanup-pass log
  (line 651) flags I-7 as "left as a frozen historical record; this pass supersedes it per G12.4," and
  the live task body uses the current names/codes throughout (verified grep-clean). No executor reads the
  superseded log entries as authoritative, so this is cosmetic.
- fix (optional): annotate the two stale log lines inline (e.g. "[superseded — see cleanup pass]") so a
  casual reader is not misled. Not required for correctness or buildability.

## Verdict

**COMPLETE.** The revised P2 plan passes all four verification axes:

1. **Coverage — zero UNCOVERED.** Every P2-scoped op/verb, header/frame field, error code, feat
   use-site, cap, `ev` type, lifecycle event, and CPU seam has >=1 concrete cited test. 11 G3 codes are
   concretely produced + tested; `denied` is correctly RESERVED (not produced); the remainder are owned
   by P1/P4 and correctly not exercised.

2. **Addendum conformance.** (a) No test asserts a positive `denied` on a cross-token collision — the
   two former unsound tests are rewritten to coexistence (`vp_same_raw_id_two_tokens_coexist`) and
   matches-nothing (`sub_cross_token_matches_nothing`), each asserting **no** `denied`. (b) Cross-token
   camera/root refs late-bind to default per G11(c) (`vp_cross_token_camera_ref_late_binds_default`)
   with the no-existence-oracle invariant pinned (G11(b)); P2 introduces no patch-ref ops. (c) Error
   codes match the G12 table — notably `instanced_node_tint → kind_conflict` per G12.4 (the `bad_param`
   synonym is struck in the live body), and the 3 RESERVED codes are emitted nowhere.

3. **DEDUP duty — applied, not re-implemented.** No second `instance.rs` decoder; `decode_instances` +
   `InstanceBuffer` + all length/NaN/cap/`count=0` rules attributed to P1-T12. T16/T17 recast as thin
   consumer + use-site policy (surface-through-accessor tests, atomic-retention + kind-invariant). P2
   owns ONLY the GPU render path (T19) and the `node.instance_set` re-tint (T18), both operating on the
   P1-T12-decoded buffer.

4. **Correctness.** Dependency graph machine-verified acyclic with no missing nodes; all edges
   backward-only (T36-before-callers inversion is documented and DAG-backward). Tests are concrete
   (named byte sequences, named accessors, named seams) — no vague tests, no invented G3 spellings
   (all codes are closed-enum members with G12.3-matching triggers). No G-invariant contradiction
   remains: the former T1↔T8 mutual-exclusion contradiction is resolved, G6 GPU-free contract is honored
   (CPU↔CPU pick parity), G9 dirty-flag gating is preserved (instance_dirty ≠ asset_revision; explore
   sets per-node dirty not per-frame scene_revision).

Open issues: 1 (P2-OBS-1, low, cosmetic — superseded names in frozen historical logs only; live body is
clean). High+medium (critical): 0. Dedup + addendum both landed. The single low observation is
documentation hygiene in the historical logs and does not affect buildability, coverage, or correctness.
