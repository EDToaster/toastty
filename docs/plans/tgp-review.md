# TGP resolution review (adversarial, per cluster)

## WIRE  (consistency_ok=False)

**Issues:**
- [high] WIRE-10: Opt-in mechanism for error reporting directly CONTRADICTS the cross-cluster owner. WIRE-10 gates emission on 'the app having negotiated error reporting (capability flag, e.g. feat=event/a dedicated errors flag)'. But CAPS-6/CAPS-7 (in SEC.md) and the register line 27 resolve the OPPOSITE: 'a completed tgp;q/tgp;r handshake IS the error-reporting opt-in' (no dedicated flag; dumb readers never handshake so stay silent, with an optional errors=0 opt-out). WIRE-10's 'feat=event' suggestion is also wrong on its face: feat=event is for INPUT events (tgp;e), not error replies (tgp;x) — conflating them. This is the load-bearing gate for WIRE-3/9/10/11 emissions, so getting it wrong silently breaks every other WIRE test that asserts an x reply.  → FIX: Adopt the CAPS-6/7 resolution verbatim: error reporting is enabled for the rest of the session once the app completes a valid tgp;q (handshake = opt-in), with an explicit errors=0 opt-out; never gate on feat=event or invent a separate errors flag. Rewrite WIRE-10's gating clause and test (b) so 'not negotiated' means 'never handshook', and cite CAPS-6/CAPS-7 as the owner.
- [high] WIRE-3: The rule 'the decoded CBOR item MUST consume the ENTIRE buffer; trailing bytes after a complete item -> parse_error;trailing_bytes' collides with the forward-compat principle and §7.2/§8.6, which say unknown ops/fields inside a patch are SKIPPED not fatal, and bulk fields ride as CBOR byte sub-blobs. CBOR encoders legitimately emit a single self-delimiting top-level item, so a strict whole-buffer rule is mostly fine — BUT WIRE-3 never reconciles with the possibility of CBOR tag/indefinite-length encodings or an encoder that pads, and more importantly it does not state that the item must be a single top-level CBOR data item (a sequence of 2 valid items would currently be 'trailing bytes'). The bigger gap: 'trailing_bytes' and 'truncated' are invented detail strings not in the closed error-code register (SEC-5/CAPS-5 pins the enum at cap_exceeded/parse_error/msg_too_large/truncated/bad_ref/dup_id/unknown_op/unsupported/vram).  → FIX: State that the buffer MUST decode to exactly one top-level CBOR data item with no trailing bytes; keep field/op-skipping forward-compat INSIDE that item (per §7.2). Map outcomes onto the SEC-5 closed code enum: use code=parse_error with detail as advisory only (detail is explicitly non-load-bearing per SEC-5), and do NOT assert on detail strings in tests as if they were normative.
- [medium] WIRE-5: WIRE-5 keys reassembly by (token, asset_id) citing F1, but the namespace token is owned by LIFE-6 (and SCEN-8 for id hardening), and the token's existence/shape/negotiation is resolved THERE, not in WIRE. WIRE-5 silently assumes a per-token addressing model exists at the chunk-buffer layer. If LIFE-6's resolution makes the token OPT-IN (tokenless writers share one default namespace, per F1's 'tokenless or untrusted writers cannot address... a different token'), then two tokenless writers BOTH using asset id=1 land in the SAME (default-token, 1) bucket and WILL collide/abort each other — exactly the corruption vector WIRE-5 claims to prevent. WIRE-5 does not define the tokenless/default-namespace key.  → FIX: Make the key (namespace_token_or_default, asset_id) and explicitly state the tokenless-writer behavior: tokenless frames share one default namespace, so concurrent tokenless uploads on the same asset id legitimately abort-on-reopen per the same rule (this is acceptable and safe). Cross-reference LIFE-6 as the token owner and note the dependency; do not assert F1 isolation guarantees that only hold for tokened writers.
- [medium] WIRE-4: Two problems. (1) The capability advertisement edit adds a NEW feat token 'bintrailer' and reorders enc= (b64 first) — but CAPS-3 (feat token set freeze) and SEC/CAPS-9 (caps reply is a closed set) own the §7.1 vocabulary. WIRE-4 unilaterally extends feat= without coordinating with the cluster that froze it; the design doc's current enc=bin,b64 lists bin FIRST (line 267), and WIRE-4 flips ordering to b64-first as normative, which is a cross-cluster caps change. (2) test_note (e) 'trl=1 not negotiated but sent -> reject as parse_error unknown-trailer' is underspecified/hand-wavy: if bintrailer was not negotiated the terminal does NOT read a trailer (WIRE-1 base framing), so after consuming len bytes the 8 trailer bytes become the START OF THE NEXT FRAME (TGPB magic + len-echo parsed as text/escape) — the test cannot assert 'unknown-trailer parse_error' because there is no trailer-read state to trip; it would instead desync or mis-parse. The decision body contradicts itself here (base framing reads no trailer, yet the test expects a trailer-specific error).  → FIX: Coordinate the feat/enc-order change with CAPS-3/CAPS-9 (note it as a cross-cluster dependency, do not assert it unilaterally). Fix test (e): with bintrailer un-negotiated and trl=1 sent, define behavior precisely — either trl=1 in the header when bintrailer wasn't negotiated is itself a header-validation error (x;parse_error;detail=bad field, emitted BEFORE reading the body, like WIRE-7 field_order), OR trl is ignored and the trailer bytes are consumed-as-next-input. Pick one and make the test assert the resulting internal state (queued reply OR next-frame parse), not a vague 'reject'.
- [medium] WIRE-1: The optional-ST tolerance interacts badly with WIRE-7's enc=b64 default and with multi-byte 8-bit ST. (1) 0x9C is a valid C1 control that also terminates a kitty/other APC; tolerating a single trailing 0x9C as a no-op after a bin frame is fine, but the decision says 'a second consecutive ST is treated as ordinary input' — for 0x9C 'ordinary input' in Ground means a C1 control execute, and for 0x1B it starts AfterEsc. test (c) asserts 'second ST routed to vte' but the current parser (parser.rs) has no path where a lone 0x9C in Ground is forwarded to vte as anything but an execute; the test note is imprecise about WHICH internal state proves the assertion. (2) The decision never says whether the optional ST tolerance applies in DISCARD mode (WIRE-8 over-cap drain) — after draining N bytes, is a trailing ST also tolerated? Unspecified.  → FIX: Specify the trailing-ST tolerance as: after the Nth raw byte, peek exactly one byte; if it is 0x1B-followed-by-0x5C, or 0x07, or 0x9C, consume it and return to Ground; otherwise return to Ground WITHOUT consuming and let normal Ground processing handle it. Make test (c) assert the concrete accessor (e.g. the byte became an execute(0x9c) record or started AfterEsc), not 'routed to vte'. State explicitly that the same single-ST tolerance applies after a WIRE-8 discard drain so the two paths are uniform.
- [medium] WIRE-8: BIN_HARD_CEIL behavior is internally inconsistent with WIRE-1/WIRE-9 resync guarantees and is undertested. When len > BIN_HARD_CEIL the decision says 'return to Ground at the header boundary WITHOUT draining'. But the header APC has ALREADY been closed by its ST (WIRE-1) and the parser was about to enter binary-read state; 'return to Ground without draining' means the N announced raw bytes (which ARE arriving on the wire) are now interpreted as text/escapes — i.e. the exact stream-desync / binary-leaks-into-grid corruption that WIRE-3 and principle 3 exist to prevent. Declaring 'unrecoverable desync' is honest but the decision simultaneously claims to keep the stream synced everywhere else; this is the one place it knowingly desyncs, and it is not flagged as such in the doc_changes for §15.2. Also 'detail=unrecoverable' is not in the SEC-5 closed code enum.  → FIX: Acknowledge in the decision and doc that the BIN_HARD_CEIL branch INTENTIONALLY accepts desync as the lesser evil vs an attacker-forced multi-GB drain, and that recovery requires a host-side reset (RIS, per LIFE-2). Use code=cap_exceeded (in the enum) and treat detail=unrecoverable as advisory only. Add a test asserting the parser is in Ground AND that subsequent bytes are (correctly, by design) treated as text — i.e. assert the desync is the defined behavior, not silently hoping for resync.
- [low] WIRE-9: test (a) asserts 'intermediate &self accessor shows remaining decreasing (20->12->4->0)' — this is a good PTY-seam internal-state assertion, but the decision puts the binary-read state in the Parser (toastty-parser crate) while remaining/buf are private fields; there is currently NO &self accessor on Parser exposing remaining (parser.rs Parser struct exposes nothing, and its Debug is finish_non_exhaustive). The test as written cannot compile without a new test-only accessor that the decision does not mandate. Same gap for WIRE-8 test (d) 'buffer never allocates beyond cap'.  → FIX: Add to the decision an explicit requirement to expose a test-only accessor (e.g. #[cfg(test)] or pub(crate) fn binary_remaining(&self)->Option<u64> and buffer len/capacity) on Parser, since the TESTABILITY IMPERATIVE requires asserting internal state via &self accessors with no GPU. Without mandating the accessor, the cross-read tests are unimplementable as specified.
- [low] WIRE-2: ApcEnd enum naming/return-type change is correct and matches grounding, but the decision says 'The Perform impl (Term layer) parses the tgp header during apc_chunk/apc_end and returns BinaryFollows from apc_end' — this requires changing the Perform trait signature for apc_end across ALL implementors (perform.rs has a default no-op apc_end and VteAdapter/AdapterBuffer forward it; the test Recorder in parser.rs also impls it returning ()). The decision understates the blast radius: every apc_end impl in the workspace (default trait method, the buffering adapter at perform.rs:117, and the kitty/RGP demux) must change return type. Not wrong, but the doc_changes don't note the trait-wide migration.  → FIX: Note that changing apc_end's return type touches the Perform trait default, the AdapterBuffer/VteAdapter forwarders (perform.rs ~117), and existing kitty/RGP demux impls; the default should return ApcEnd::Done so non-tgp impls are unaffected. Add this migration note to the doc_changes/grounding callout.
- [low] WIRE-6: Sound and well-aligned with §8.6 atomicity, but it overlaps CAPS-3 (chunk reassembly cap teardown: check-per-chunk vs at-assembly, immediate free) without cross-referencing it. WIRE-6 says 'any failure discards the whole buffer' but does not say WHEN the cap is checked (per incoming chunk vs at final assembly) — CAPS-3 owns that and recommends per-chunk check with immediate free. If WIRE-6 implies at-assembly-only checking it contradicts CAPS-3's per-chunk model and reopens the parse-time-bomb (buffer grows to max_reasm before any check).  → FIX: Add a sentence deferring cap-check TIMING to CAPS-3/WIRE-8 (per-incoming-chunk cumulative check, immediate free on breach) so the two resolutions agree, and cross-reference CAPS-3 as the owner of teardown timing.

**Cross-cluster:**
- ERROR OPT-IN (WIRE-3/4/5/6/8/9/10/11 ALL depend on it): The owner is CAPS-6/CAPS-7 (in SEC.md) + register line 27 — 'a completed tgp;q/tgp;r handshake IS the error-reporting opt-in', NOT a feat flag. WIRE-10 contradicts this. Every WIRE test that drains a tgp;x reply assumes the gate; if the gate resolution differs, all those tests change. Reconcile WIRE-10 to the CAPS resolution.
- ERROR-CODE ENUM: SEC-5/CAPS-5 pins a CLOSED versioned code enum (cap_exceeded, parse_error, msg_too_large, truncated, bad_ref, dup_id, unknown_op, unsupported, vram) with detail strings explicitly NON-load-bearing/advisory. WIRE invents detail strings (trailing_bytes, truncated, chunk_interleave, chunk_fmt_mismatch, field_order, trailer_corrupt, base64, max_msg, max_reasm, unrecoverable) and several of its test_notes ASSERT on detail= as if normative. Either get these details added to the enum spec or rewrite tests to assert only on code= (the load-bearing part). Note WIRE uses 'parse_error' where CAPS-1/SEC-1 uses 'msg_too_large' for the cap case — WIRE-8 uses cap_exceeded which is consistent, but verify msg_too_large vs cap_exceeded naming is unified across clusters.
- NAMESPACE TOKEN (F1): Owned by LIFE-6 (multi-process scene, opt-in token) + SCEN-8 (id hardening, reserved prefix). WIRE-5 and WIRE-10 consume per-token keying/routing but the token's negotiation, shape, and tokenless-default behavior are resolved in LIFE-6. WIRE must defer the token model to LIFE-6 and only specify the chunk-buffer key tuple and reply-routing consumption. Critical edge: tokenless writers share a default namespace (per F1) so WIRE-5's collision-isolation guarantee does NOT hold between two tokenless writers — must be stated.
- TXN-IN-TEXT-HEADER: WIRE-10 correctly hoists txn into the text header, which is the SAME resolution as CAPS-1/SEC-1 (register line 23: 'hoist txn into the text header so cap-rejections cite it & still drain N bytes') and WIRE-8's drain behavior. These are consistent — good — but WIRE should cross-reference CAPS-1 as co-owner so the header field order (txn/enc/len ordering from WIRE-7) is jointly specified. WIRE-7 mandates enc-before-len but is SILENT on where txn sits; CAPS-1 needs txn present pre-body. Specify txn ordering too.
- CAPS-8 (malformed near-miss tgp; prefix): Resolves that a malformed tgp; frame in a native session emits x rather than silent-drop, and unknown-prefix APCs stay silent. WIRE never addresses what happens to an APC whose prefix is almost-but-not tgp; or a tgp; control frame with an unknown type — these route through apc_end returning Done (WIRE-2) and then... unspecified in WIRE. Align with CAPS-8 so detection failures are diagnosable.
- FEAT/CAPS REPLY VOCABULARY: WIRE-4 adds 'bintrailer' to feat= and WIRE-8 adds max_reasm_mb to §7.1, and WIRE-4 reorders enc= to b64-first. CAPS-3 (feat token freeze + implication rules) and CAPS-9/SEC-9 (closed caps set with explicit scope) own §7.1. These WIRE additions must be merged into the CAPS-owned reply schema, not asserted independently, or the two clusters will emit divergent capability replies. Note current doc (line 267) lists enc=bin,b64 (bin first) and feat without bintrailer.
- CAPS-5-in-CAPS.md vs CAPS-7-in-SEC.md NAMING COLLISION: The findings file SEC.md actually contains findings labeled CAPS-1..CAPS-10 (caps enforcement/errors), while CAPS.md contains the handshake-negotiation CAPS-1..CAPS-8. There are TWO different 'CAPS-5', 'CAPS-6', 'CAPS-7' etc. across the two files. When WIRE cites a cross-cluster id, it must disambiguate (e.g. the error-enum is SEC.md/CAPS-5; the error opt-in is SEC.md/CAPS-6 AND CAPS.md/CAPS-7 — both say handshake-is-opt-in). The resolver should normalize cluster ids to avoid mis-wiring dependencies.

**Missed:**
- MALFORMED HEADER / unknown header fields: WIRE-7 handles enc-before-len order, but no WIRE decision covers a header with an UNKNOWN field, a missing/garbage len= on enc=bin, or a non-numeric len. Per §7.2 unknown FIELDS are skip-tolerant, but a missing/garbage len on enc=bin is unrecoverable (parser can't know how many bytes to consume) — must be a parse_error that does NOT enter binary state. This is the most dangerous unhandled header case (it decides whether the raw bytes leak into the grid) and is unspecified.
- CONTROL FRAME vs BINARY FRAME demux for type: WIRE-2 says non-bin/control frames return Done, but §8.5 lists vp as 'control/binary' — a vp frame CAN be enc=bin. WIRE-2/7 implicitly assume only type=p uses enc=bin. No decision states which message TYPES may carry enc=bin (p, vp?) vs are always control (q/r/e/x/a/sub/anim). An enc=bin on a type that should be control-only is unhandled.
- DETERMINISTIC CLOCK / animation: The TESTABILITY IMPERATIVE and grounding both flag RGP animation uses real-Instant ticks (non-deterministic) and require an injectable deterministic clock. No WIRE decision is about animation, which is correct (that's ANIM cluster), BUT WIRE-9's EOF-handling and any timeout-based partial-frame abort (CAPS-2 proposes an inactivity timeout for stalled partial frames) would need the deterministic clock. WIRE-9 handles EOF but NOT the stalled-partial-frame DoS (slow-drip huge len) that CAPS-2 raises — WIRE-9 only discards on actual EOF, leaving the hung-curl-drips-bytes-forever vector to CAPS-2. WIRE should cross-reference CAPS-2 or note the stall case is out of WIRE scope.
- b64 WHITESPACE / line-folding: WIRE-11 covers base64 decode errors (detail=base64) but does not address whether embedded whitespace/newlines in the b64 payload (common when frames are line-folded for transport) are tolerated or rejected. Real base64-over-APC often contains soft line breaks. Unspecified — affects whether the 'app pre-trims to ceil(cap*4/3)' bound is in raw chars or non-whitespace chars.
- RIS/reset interaction with in-flight binary-read and chunk buffers: LIFE-2 resolves RIS = full scene teardown INCLUDING pending chunk buffers. But what if RIS (ESC c) arrives while the parser is MID binary-read (remaining>0)? The ESC of RIS would be consumed as raw payload bytes (binary-read disables escape scanning per WIRE-1), so RIS cannot interrupt an in-flight binary frame. This is a real safety gap: an attacker who sends a huge len then never finishes wedges the parser AND RIS can't recover it until the byte count completes or EOF. WIRE-9 + LIFE-2 together leave this uncovered; WIRE-8's BIN_HARD_CEIL only helps if len itself is over-ceiling.
- ack (tgp;a) timing for chunked/binary frames: §8.5 defines tgp;a ack for a committed txn. WIRE-6 says the completing chunk frame carries the txn cited in ack/x, but no WIRE decision specifies whether/when an ACK is emitted for a successfully applied binary patch (vs only errors). LIFE/CAPS may own acks, but WIRE-3/6/10 mention ack routing without defining ack emission — note the gap or defer explicitly.
- Parser back-channel for enc=b64 size enforcement: WIRE-11 says b64 flows through the existing APC chunk path bounded by min(encoded-cap, 256MiB APC cap). But the existing apc_chunk path (term.rs apc_buffer, APC_BUFFER_CAP=256MiB at term.rs:3380) has NO per-message encoded-cap today — WIRE-11 requires NEW logic to bound the APC accumulation at ceil(max_msg_mb*4/3) specifically for tgp b64 frames while leaving kitty/other APCs at 256MiB. The decision doesn't specify HOW the apc layer distinguishes a tgp b64 frame (needs header peeking) from a generic APC to apply the tighter cap — this is a real implementation gap mirroring the WIRE-2 back-channel problem but for the b64 path.

## EXPL  (consistency_ok=False)

**Issues:**
- [high] EXPL-7: The decision and rationale claim 'Mandated by USER DECISION F7' / 'this is a USER DECISION finding' — but there is NO F7. The user's binding forks are only F1-F4. EXPL-7 is correctly tagged [USER DECISION] in the findings doc (it is one of the deferred forks rolled into F4's family per the register), but none of F1-F4 actually specify camera-report TRS schema, settle-on-end, or <=10Hz cadence. So this resolution is dressing up resolver judgment as a non-existent user mandate. The substance (TRS matching node.upsert, throttled, never per-frame, coalesce) is good and consistent with principle 3/5, but the provenance is fabricated.  → FIX: Strike the 'USER DECISION F7' citation. Re-justify EXPL-7 purely on principles (3 bounded back-channel, 5 compact wire, 6 revision generalization) and as resolver judgment over the EXPL-7 finding's recommendation. Cite the real recommendation in EXPL.md line 57, not a phantom fork.
- [high] EXPL-1: test_notes drive everything via 'feed synthetic press at (c,r)', 'inject press then move', 'Move-only (no button)'. These pointer gestures do NOT arrive through Parser::advance(&mut Term, bytes) — that seam is the PTY app-output byte stream (sub/patch/node ops). User pointer/wheel/focus input enters from the windowing layer, not the PTY. The TESTABILITY IMPERATIVE names Parser::advance as the input seam and 'injectable deterministic clock' for time, but never sanctions a pointer-injection seam, and this resolution silently assumes one without naming the accessor. Only the 'sub requesting raw+events -> tgp;x invalid_sub' sub-case (a) is genuinely a Parser::advance byte test.  → FIX: Name the concrete CPU-side input-injection accessor (e.g. Term::handle_pointer(PointerEvent)/Term::handle_wheel/Term::handle_focus) as the deterministic input seam, parallel to the injectable clock, and state that ONLY sub/patch/node arrive via Parser::advance. Make every gesture test_note assert via &self accessors (camera node TRS, queued pty_replies) after driving that accessor. Apply the same fix across EXPL-4/5/6/7/8/10 which share this gap.
- [medium] EXPL-1: invalid_sub is introduced as a tgp;x code by EXPL-1, EXPL-9, EXPL-10 but it is NOT in any error-code enum. CAPS-5/SEC-5 mandate a CLOSED versioned error-code enum (cap_exceeded, parse_error, msg_too_large, truncated, bad_ref, dup_id, unknown_op, unsupported, vram). invalid_sub is absent. A closed enum that silently grows per-cluster defeats the closed-enum contract.  → FIX: Coordinate with the CAPS/SEC resolver: add invalid_sub (and any other new EXPL codes) to the canonical closed enum in one place, or reuse an existing code. State the dependency explicitly in EXPL-1/9/10 doc_changes.
- [medium] EXPL-9: Marked 'MOOT in part' and open:false, but EXPL-9 is NOT an RGP-reconciliation finding — F2's MOOT clause only applies to findings that exist solely to reconcile RGP with TGP. EXPL-9 is a genuine raw-vs-reflow/compositing contradiction in §12.2/10.3/10.4 that must be RESOLVED, not declared moot. Conflating 'MOOT (F2 sense)' with 'resolved by scoping' muddies the register's MOOT bookkeeping. The actual scoping resolution (raw = pointer-forward only, terminal keeps reflow/compositing, explore implicitly off) is correct and should stand as a normal resolution.  → FIX: Drop the 'MOOT in part' framing. Keep open:false but label it 'resolved' (contradiction removed by scoping), not moot. Reserve 'MOOT' strictly for RGP-only findings per F2.
- [medium] EXPL-3: The whole resolution leans on 'Subscriptions are owned by the namespace token (F1)' and cross-token isolation (test e), but the token MECHANISM (handshake negotiation, the §8.4 namespace field, how a sub frame carries/derives its token) is owned by LIFE-6/CAPS-10/SCEN-8, not EXPL, and does not yet exist. EXPL cannot assert token-scoped routing as normative until that mechanism is pinned. test (e) 'T2 sends sub for A -> never matches T1's node' is untestable without a defined way to attribute an inbound sub frame to a token on the Parser::advance seam.  → FIX: Add an explicit cross-cluster dependency note: EXPL token-routing is contingent on LIFE-6/CAPS-10 defining (1) how a token is negotiated at handshake and (2) how each inbound frame on a single PTY is attributed to a token. Until then, mark the token-scoping clauses as 'pending F1 mechanism'. The non-token lifecycle rules (auto-cleanup, leave-on-remove, dormant sub) can stand independently.
- [medium] EXPL-5: EXPL-11 declares ev=resize NON-coalescable and lists it among events, and EXPL-5 governs the hover state machine, but neither resolves the interaction between viewport reflow/resize (VIEW-4/F3 placeholder-cell reflow) and hover: when reflow moves the placeholder rect out from under a still cursor, does that synthesize leave? EXPL-5 only enumerates removed/hidden/moved-via-explore-anim-patch; reflow-driven motion (the F3 headline path) is not explicitly covered as a hover-recompute trigger.  → FIX: Add reflow/resize-driven rect motion to EXPL-5's recompute triggers (recompute hover on any change to the live placeholder rect, not just explore/anim/patch), and add a test: scroll/reflow moves the rect off a stationary cursor -> ev=leave synthesized. Cross-ref F3/VIEW-4.
- [medium] EXPL-4: Drop-on-missing-node is specified for click/hover/enter and leave is exempted, but EXPL-4 stamps rev= (the scene_revision the pick was resolved against) on EVERY tgp;e, while EXPL-5/6 synthesize leave/up/enter events that are NOT resolved against a pick buffer at a specific revision (they are state-machine-synthesized). The rev= field semantics for synthesized events are undefined — what revision does a synthesized leave-on-remove carry, given the node is gone at the bumped revision?  → FIX: Define rev= for synthesized events explicitly: a synthesized leave/up carries the revision at which the held target was last valid (pre-removal), and enter-from-state carries the current scene_revision. State that rev= on synthesized events is the revision the cited node id was last resolvable against.
- [low] EXPL-2: The 'both=1' hybrid flag is placed on the sub (per-subscription), but F4 describes it as an app-level opt-in ('lets a hybrid app receive both SGR and tgp;e'). Per-sub vs per-app/per-session granularity is a real semantic choice (a hybrid TUI likely wants it session-wide, not re-declared on every sub). Resolution picks per-sub without justifying it against F4's app-level wording.  → FIX: Either justify per-sub granularity (finer control, default 0) explicitly against F4, or move both to a session/handshake-level flag. State the choice and rationale so it is not silently narrower than F4 implies.
- [low] EXPL-8: Opaque-empty-consumes rule: 'if its clear is opaque, it owns the event (consumes even empty space)'. This couples input ownership to the clear color being opaque, but VIEW-9 notes clear=None vs Some governs whether the offscreen punches a hole over text; an opaque-clear viewport with no geometry visually shows its clear color (occludes), so consuming is consistent. However EXPL-8 does not address the case where the opaque viewport is itself transparent-composited at a lower alpha, nor pinned-vs-inline z-tie (VIEW-8 leaves z-ties unresolved). A z-tie between two subscribed overlapping viewports has no deterministic input winner here.  → FIX: Cross-ref VIEW-8 and state the tie-break for equal-z subscribed viewports (e.g. later-created wins, matching the compositing tie-break VIEW-8 must pick) so input ownership is deterministic. Note dependency on VIEW-8's z-tie resolution.
- [low] EXPL-6: Damping-to-rest on focus-out 'using the injectable deterministic clock' depends on ANIM-1's injectable monotonic clock, and the grounding flags RGP's real-Instant ticks as non-deterministic. EXPL-6 (and EXPL-7's throttle, EXPL-1's orbit) assume the deterministic clock is wired into the EXPLORE/damping path, but ANIM-1 only mandates it for anim PLAYBACK. There is no stated guarantee the explore controller and back-channel throttle share that same injectable clock.  → FIX: Add an explicit dependency: the explore damping integrator AND the camera-report throttle MUST use the same injectable monotonic clock interface defined by ANIM-1, not wall-clock Instant. State it in EXPL-6/EXPL-7 so determinism is guaranteed end-to-end.

**Cross-cluster:**
- F1 TOKEN MECHANISM (LIFE-6/CAPS-10/SCEN-8): every EXPL resolution asserts 'token-routed (F1)' but the token's negotiation (handshake field), the §8.4 'one global namespace per session' rewrite, and per-frame token attribution on a single PTY are owned by LIFE/CAPS, not EXPL. EXPL is fully dependent on those resolutions landing; EXPL-3 test (e) cross-token case is untestable until then.
- DESIGN DOC §8.4 line 340 literally says 'one global namespace per session for the scene' — this directly contradicts F1 and MUST be rewritten by whoever owns the namespace/token resolution. No EXPL doc_changes entry flags this required edit.
- ERROR CODE ENUM (CAPS-5/SEC-5): EXPL introduces tgp;x code=invalid_sub (EXPL-1/9/10) which is absent from the closed versioned enum CAPS-5/SEC-5 mandate. Must be added centrally or the closed-enum contract breaks.
- INJECTABLE CLOCK (ANIM-1): EXPL-1 orbit, EXPL-6 damping, EXPL-7 throttle all require the deterministic injectable clock, but ANIM-1 currently scopes it to anim playback. The explore controller + back-channel throttle must share that clock; grounding confirms RGP uses non-deterministic real-Instant ticks that must NOT leak into explore.
- PLACEHOLDER REFLOW (F3/VIEW-2/VIEW-4): EXPL-2/8/10 correctly key capture to the live placeholder cell rect, but EXPL-5's hover state machine omits reflow/resize-driven rect motion as a leave trigger. VIEW-4 (placeholder cells authoritative post-reflow) is the dependency. Also VIEW-2's max_inline cap rejection (tgp;x) is the model EXPL-1/10's invalid_sub rejection pattern should mirror.
- Z-TIE (VIEW-8): EXPL-8 input hit-test walks top-down by z but VIEW-8 leaves equal-z and pinned-vs-inline ties unresolved; EXPL-8 inherits that ambiguity for input ownership and must adopt VIEW-8's eventual tie-break.
- BINARY FRAMER (WIRE-2): EXPL-11 governs the back-channel FIFO/coalescing for tgp;e/a/x but the grounding's apc_end()->Option<BinaryFollow> back-channel and WIRE-2's binframe are about the app->terminal direction; EXPL-11's 'binary framer under load' title overreaches — the terminal->app event channel (pty_replies) is text-APC, not the binary inbound framer. Clarify scope to avoid conflating the two directions.
- BACK-CHANNEL CAP (CAPS/§7.1): EXPL-7 (camera_report_hz), EXPL-1 (click_deadzone_px), EXPL-11 (max_replies/back_channel cap) all add new advertised caps to §7.1. These must be reconciled with the CAPS cluster's caps-vocabulary/freeze decision (CAPS-3) and the runtime-variable-caps decision (CAPS-4) so they are not silently static.

**Missed:**
- No resolution addresses subscription lifetime vs APP EXIT / focus / PTY input-owner change (LIFE-7): EXPL-3 covers vp-destroy/node-remove teardown but not 'the subscribing app exited / a different process now owns PTY input' — LIFE-7's rec is to cancel subs+explore+anim on foreground-process change and never emit while unfocused. EXPL-6 handles focus-out for in-flight GESTURES but not the standing question of whether events keep being emitted to a dead/backgrounded subscriber. This is squarely an EXPL+LIFE interaction left open.
- dblclick is listed in the design doc ev enum (§12.5 line 586) and EXPL-11 marks it non-coalescable, but NO resolution defines the dblclick detection rule (time window, movement tolerance, relationship to the EXPL-1 click/down/up partition). A click-vs-dblclick timing window needs the injectable clock and a deadzone, exactly like EXPL-1's click rule, yet it is unspecified.
- drag AXIS-ownership hand-off is asserted but underspecified: EXPL-1 says 'drag events are only forwarded when explore does not own that axis (orbit/pan disabled for that drag)'. With orbit on but pan off, a single drag has no per-axis decomposition defined — does a diagonal drag orbit AND forward? The 'that axis' concept is never mapped to ExploreOpts.orbit/pan/lock_axes. This is a vague unresolved edge.
- Modifier keys during gestures are never addressed: explore commonly uses shift/ctrl to switch orbit/pan, and EXPL-10 explicitly rejected modifier-gated wheel zoom as an option without saying modifiers are dropped/forwarded. Whether tgp;e events carry modifier state (the wire format §12.5 has no mod= field) is missing across EXPL.
- Pick buffer resolution vs sub-cell pointer precision: EXPL-4/8 resolve picks against a per-viewport pick buffer 'under the cursor', but the mapping from a PTY/windowing pointer pixel to the pick buffer texel (and how that survives the cell-rect->pixel-rect reflow) is unspecified — relevant because the buffer is refreshed at scene_revision cadence while the cursor moves continuously.
- No resolution states what happens to a sub that targets a viewport which does NOT exist yet (parallel to EXPL-3's dormant node-id sub). EXPL-3 says a not-yet-existing NODE id sub is valid+dormant, but a sub naming an unknown VIEWPORT id is left undefined (accept-dormant vs tgp;x invalid_sub) — and viewport-level dormancy is riskier since capture/pick allocation is keyed to the vp.

## MATE  (consistency_ok=False)

**Issues:**
- [high] MATE-4 / cross CAPS-7: MATE-4 gates the cap_exceeded report on the app having 'opted into error reporting' and its test (5) asserts behavior for 'an app that did NOT opt into error reporting' as if a per-app/per-session error toggle exists. CAPS-7's resolution (register P0, CAPS.md rec option 2) is that a COMPLETED tgp;q/tgp;r handshake IS the error-reporting opt-in; there is no separate errors=1 flag in v1. So 'opted in' == 'handshaked', and a non-handshaked writer cannot have a token to route to anyway. As written MATE-4 implies a mechanism (a distinct error-reporting opt-in) that does not exist, making test (5) un-buildable as stated.  → FIX: Reword to: 'the report is queued only to the owning F1 token's handshaked channel (a completed tgp;q/tgp;r handshake is itself the error-reporting opt-in per CAPS-7); tokenless/non-handshaked writers receive nothing.' Restate test (5) as 'a writer with no completed handshake (no token): selection still deterministic but NO x queued because there is no channel/token to route to.'
- [high] MATE-6 / MATE-8 / MATE-10 / MATE-2 — F1 routing: Only MATE-4 mentions F1 token routing. F1 requires that error replies AND acks route ONLY to the owning app/token. MATE-6 queues a viewport ack 'a;vp=...;msaa=<n>' (test 2), MATE-8 queues x;parse_error (tests 2-4), MATE-10 queues x;parse_error (test 2), and MATE-2 asserts 'NO x queued' — none of these state the ack/error is scoped/routed to the owning F1 token, nor that a tokenless writer's reply (if any) is isolated. Per F1 this is mandatory for every reply-emitting decision, not just MATE-4.  → FIX: Add to MATE-6, MATE-8, MATE-10 (and any reply path) an explicit clause: 'the ack/error routes ONLY to the owning F1 token's reply channel; a write from a different/absent token cannot receive it.' Add a test assertion that the queued reply lands in the owning token's pty-reply partition, not a shared/global one (e.g. assert reply tagged with the owning token id).
- [medium] MATE-6: The clamp-DOWN-only rule produces a worse-than-default outcome: with supported {1,4} and default 4x, an app that explicitly requests msaa=2 or msaa=3 (a deliberate quality choice) gets clamped to 1 (MSAA OFF) — strictly worse AA than doing nothing. The decision frames MSAA as 'a quality knob, not a resource', but clamp-down silently turns a request for 'some AA' into 'no AA', which is surprising and under-justified vs clamping to nearest-supported or toward the default.  → FIX: Either (a) clamp to NEAREST supported (2 or 3 -> 4 when 4 is the nearest), or (b) clamp toward the advertised default when the requested value falls between supported counts, and document the tie-rule. Whichever is chosen, state it explicitly and add the effective count to the ack so the app can react. Keep advertising the supported set so apps can avoid the situation entirely.
- [medium] MATE-6: Test note (3) contains an unresolved, hand-wavy deliberation left verbatim in a normative artifact: 'assert clamped to 1 (nearest supported <= 3) -- wait, with {1,4} nearest <=3 is 1; assert ==1.' The 'wait, ...' is an author's mid-thought, not a pinned assertion, and it exposes that the clamp semantics were not cleanly settled.  → FIX: Delete the 'wait, ...' aside and state the single expected value once, consistent with whatever clamp rule MATE-6 adopts (see prior issue). E.g. under clamp-to-nearest: 'msaa=3 with supported {1,4} -> resolved msaa==4; assert ack reports 4.'
- [medium] MATE-9 / cross SCEN-6: MATE-9 declares per-instance tint is 'sRGB-encoded 8-bit-or-float RGBA on the wire'. SCEN-6 (register P0) pins ONE canonical instance layout = 'mat4 f32 LE + RGBA8 + explicit count, bounds-checked' — i.e. per-instance tint is fixed RGBA8, not float. The 'or-float' option for the instance-tint path contradicts the pinned single instance layout and the compact-wire principle.  → FIX: Narrow MATE-9: per-instance tints are RGBA8 sRGB (per SCEN-6's fixed layout); the 'float RGBA' option applies only to non-instance color fields (node tint / material base / clear color) if float is even allowed there. Cross-reference SCEN-6 so the two decisions cannot drift.
- [low] MATE-1 / MATE-2: MATE-2 clamps default-material brightness to [0,8], so default-material linear output can far exceed 1.0. MATE-1's default tone-map operator 'none' is defined as 'passthrough clamp to [0,1]'. Therefore a default-only viewport with brightness>1 and the default operator silently HARD-CLIPS to white — a real visible behavior that the 'visually identical to a plain linear render' phrasing in MATE-1 glosses over.  → FIX: Note explicitly in MATE-1/MATE-2 that under operator 'none' linear values >1 (reachable via brightness up to 8) are clamped to [0,1] (hard clip), and that apps wanting highlight roll-off must opt into Reinhard/ACES. Add a test: default node brightness=8 with tone_map=none -> assert resolved pixel model clamps the channel to 1.0 (clip), distinct from the same scene with aces (rolls off below 1.0).
- [low] MATE-3 / MATE-4 consistency: MATE-3 disables the implicit built-in light when the viewport's visible subtree contains >=1 Light node and lights the default material 'ONLY by registered lights'. MATE-4 then selects at most max_lights of the visible lights. Edge case unaddressed: if registered lights exist (so built-in is OFF per MATE-3) but ALL of them are culled/zero-selected by MATE-4 (e.g. max_lights=0 in a degenerate cap, or all lights hidden the same frame the subtree still 'contains' a hidden Light), the default material could go fully black with no fill. MATE-3 vs MATE-4 don't define whether 'contains a Light' counts hidden/zero-intensity lights for the built-in-disable switch.  → FIX: Define the built-in-disable predicate precisely: it triggers only when >=1 VISIBLE, selectable Light remains after MATE-4 selection (not merely 'a Light node exists in the subtree'). If selection yields zero effective lights, keep the built-in default on (or document keep_default_light as the escape). Add a test: subtree has one Light but it is node.visible=false -> assert built-in key stays ON.
- [low] MATE-8: MATE-8 asserts dimension rejection 'from header before full decode' (test 3) and bounds decode by the 15.2 decode-time cap, but does not address the decompression-bomb case where the PNG HEADER declares an in-range dimension yet the IDAT inflates to a huge buffer, nor pixel-count (W*H) vs per-axis max_tex_dim. Per-axis 4096 still allows 4096x4096 = 67MP which can blow VRAM/decode-time even though each axis is 'in cap'.  → FIX: Add a max total decoded byte / pixel-count guard (not just per-axis), and reaffirm the 15.2 decode-time + reassembly-size caps abort mid-inflate with x;parse_error;detail=tex_dim (or a decode_bomb detail). Add a test feeding a small-header/large-IDAT PNG and assert the decode-time/byte cap fires with a structured error and scene unchanged.

**Cross-cluster:**
- CAPS-7 (P0): error-reporting opt-in == completed tgp;q/tgp;r handshake (no separate errors flag in v1). Every MATE decision that emits x or a (MATE-4,6,8,10) must phrase 'opt-in' as 'handshaked', and MATE-4 test (5) must be reframed accordingly — see issues.
- F1 (per-app token): error replies, acks, and event reports route ONLY to the owning token; tokenless/untrusted writers cannot receive them or address others' nodes. MATE-6 (ack), MATE-8/MATE-10 (errors), MATE-4 (error) and MATE-2 (no-error) must all carry token-routing language and tests; currently only MATE-4 does. Note design-doc §8.4 still says 'one global namespace per session' which directly contradicts F1 and must be rewritten when MATE doc-changes land.
- SCEN-6 (P0): single canonical instance layout = mat4 f32 LE + RGBA8 tint + explicit count, bounds-checked. MATE-9's 'sRGB 8-bit-OR-float RGBA' for tints conflicts for the per-instance path (RGBA8 only). MATE-10's drop of per-instance material to {transform,tint} agrees with SCEN-6 and §6.4/§16 — consistent there.
- SCEN-7 (P0): instance content change bumps scene_revision + per-node instance-dirty, NEVER asset_revision. MATE-1/MATE-10 talk about draw records / instancing but do not touch revisions; ensure MATE doc-changes to §6.4 do not reintroduce asset_revision bumps for tint-only instance edits (consistent with grounding commit 96da70a split).
- CAPS-1/2 (P0): tgp;r must precede DA1 on the shared FIFO writeback (queue_reply->pty_replies). MATE acks/errors share that exact channel; the new a;vp=...;msaa ack (MATE-6) and x replies must respect the same FIFO so they cannot be reordered ahead of the handshake reply. Worth a note that MATE acks are post-handshake so ordering is naturally satisfied.
- EXPL/F4 + MATE-7: MATE-7's 'pick target always 1x, never tone-mapped/resolved' is the correctness substrate for F4 input routing (sub captures viewport cell rect -> pick -> event). EXPL-? raises multi-viewport hit-test ordering (topmost-by-z / pierce-transparent); MATE-7 guarantees per-viewport pick correctness but the cross-viewport pick arbitration is an EXPL decision, not covered here — flag so the two don't assume each other's behavior.
- EXPL/§12.4: v1 event report includes world-space hit point (wx,wy,wz) derived from pick-pass depth. MATE-7 forbids MSAA/tone-map on the pick target but does not state the pick target's DEPTH is the authoritative source for the world point; ensure MATE-7 + EXPL agree that depth read for world-point comes from the 1x pick pass (not the MSAA color pass) so the world point is exact.

**Missed:**
- No decision pins the OFFSCREEN COLOR TARGET ALPHA / un-drawn-pixel behavior, yet MATE-1/MATE-9/MATE-11 all end at 'composite vs text plane'. VIEW (VIEW-? transparent-vs-clear) raises that clear=None / un-drawn pixels are undefined (text bg vs alpha=0 vs cell bg). MATE-1's 'sRGB-encode into offscreen then depth-tested composite' is incomplete without the alpha/blend rule. MATE should at minimum cross-reference the VIEW compositing decision and state whether the tone-mapped+sRGB-encoded target is premultiplied-alpha so the composite math is defined; otherwise the 'composite matches text automatically' claim (MATE-9) is unproven for transparent regions.
- Theme-tint (MATE-5) reads the active palette foreground 'once per frame', but no decision addresses LIVE THEME CHANGE / OSC palette reconfiguration mid-session forcing a re-render. With the scene_revision/asset_revision split (principle 6, grounding), a palette change that alters theme-tinted default-material color is neither a scene nor asset edit — it needs its own dirty trigger or theme-tinted viewports won't update. MATE-5 should specify that a default-fg palette change marks theme_tint viewports viewport-dirty (or bumps a render-input revision). A PTY test: send theme-tint viewport, then change palette fg via the injectable palette, assert the viewport is marked dirty / resolved diffuse-input recomputed.
- No decision states how default-material nodes that DO respond to registered lights (MATE-3) interact with theme-tint (MATE-5) ordering when lights are present: MATE-11 fixes theme-tint(diffuse input)->lighting, which is fine, but MATE-3's 'registered lights only' path plus MATE-5's diffuse multiply means theme-tint now modulates the diffuse term that registered lights illuminate — confirm that is intended (a blue theme tints a sun-lit default node's albedo) and add a test combining theme_tint=true + a registered Light, asserting resolved diffuse-input is fg-multiplied AND lit by the registered light.
- max_lights default VALUE is never specified. MATE-4 advertises max_lights and doc-changes show 'max_lights=8' only in an example; unlike MATE-6 (msaa default 4x) and MATE-8 (max_tex_dim 4096 example), MATE-4 gives no normative default count. Pin it (e.g. 8) so implementers don't diverge and so the deterministic-selection tests have a fixed cap.
- MATE-2 pins the key light direction in VIEW space (normalize(-0.3,-1.0,-0.4)), but with explore (F4/§12.3) moving the CAMERA node, a view-space key light means the default lighting rotates with the camera (headlight-like) — which may or may not be intended vs a world-space default sun. This is a visible design choice left implicit. State explicitly whether the built-in key is view-space (follows camera) or world-space, and add a test: same node, two camera poses, assert built-in key direction in the resolved per-node lighting is identical-in-view-space (view) vs identical-in-world-space (world).

## ANIM  (consistency_ok=False)

**Issues:**
- [high] ANIM-4 / ANIM-6: Direct internal contradiction on stop/end pose. ANIM-4 says stop 'resets the node's owned channels to the clip's bind pose' and parenthetically defines bind pose as 'its values at t=0 of the node's local transform prior to playback start'. That is the PRE-PLAYBACK local transform, NOT the glTF bind pose, and NOT the clip sample at t=0 (a clip sample at t=0 is usually different from the node's authored transform). ANIM-6 then says a non-looping clip at end 'does NOT reset to bind pose' and holds the last frame. So the term 'bind pose' is used with three different meanings across the two decisions (glTF bind pose / pre-playback local TRS / clip sample at t=0), and ANIM-7 separately uses 'bind pose' to mean the glTF skin bind pose. A test cannot assert 'pose == bind pose' because the spec does not pin which value that is.  → FIX: Pin one term: define 'restore TRS' = the exact per-channel local-transform values the owned channels held immediately BEFORE this playback instance started (snapshot them at play time). State stop restores those snapshotted values; Ended/anim_end holds the last clip sample and does NOT restore. Reserve 'bind pose' exclusively for the glTF skin bind pose (ANIM-7). Update ANIM-4 doc_changes and test (6) to assert against the snapshotted pre-play TRS, not an ambiguous 'bind pose'.
- [medium] ANIM-4 / ANIM-6: State-machine enum mismatch. ANIM-4 defines the state machine as exactly {Stopped, Playing, Paused} and says seek/pause/stop on an unknown pair -> not_playing. ANIM-6 introduces a fourth terminal 'Ended substate (still Paused-equivalent)' and ANIM-9 keys animation_deadline() off 'Playing' only, returning None for 'Paused/Ended'. Ended is never listed in ANIM-4's enum, and it is unclear whether seek-out-of-Ended is legal, whether play on an Ended pair restarts (ANIM-4 says play on Paused restarts; is Ended a Paused?), and whether stop from Ended restores pre-play TRS. The state graph is incomplete.  → FIX: Add Ended to the ANIM-4 state enum explicitly with all transitions: Ended -> [play] restart->Playing; Ended -> [seek<duration] -> Paused (re-armed) or stays Ended at duration; Ended -> [stop] -> Stopped (restore pre-play TRS); Ended -> [pause] no-op. State that Ended retains channel ownership (matches ANIM-6). Make ANIM-4, ANIM-6, ANIM-9 reference the identical 4-state enum.
- [high] ANIM-2 / ANIM-10: anim_stop directive ordering is contradictory and the wire encoding is unspecified. ANIM-2 allows a node.upsert to carry 'anim_stop=true' OR be 'preceded in receive order by anim;stop' to release ownership before applying. But ANIM-10 states emphatically that 'anim is never part of a patch txn' and frames are strictly receive-ordered and atomic. A node.upsert is an op INSIDE a patch txn (8.6), while anim;stop is a separate frame; if anim;stop precedes the patch in receive order, ownership is already released and the in-patch anim_stop=true flag is redundant — but if anim;stop FOLLOWS in the same write, the patch with the conflicting channel has already atomically rejected with node_busy before anim;stop is seen. So 'preceded in receive order' is the only viable path and the anim_stop=true field is a NEW per-op field on node.upsert that is not in the 8.6 op schema. Test (5) 'anim;stop then node.upsert in same byte stream' only works in that exact order; reverse order silently fails the assumption.  → FIX: Drop the dual mechanism. Either (a) make anim_stop=true a defined optional field on the node.upsert op schema (add it to 8.6) processed atomically as the first sub-step of that op so the txn does not self-reject, and remove the 'preceded by anim;stop' alternative as just normal ordering; or (b) require apps to send anim;stop as a prior frame and remove anim_stop=true entirely. Pick one and make the test assert the failing reverse-order case explicitly (anim;stop AFTER the patch -> node_busy).
- [medium] ANIM-6: seek-past-duration on a non-looping clip is underspecified for the Playing case and conflicts with ANIM-4 seek semantics. ANIM-6 says 'seek past duration on a non-looping clip clamps to duration and is treated as Ended.' ANIM-4 says 'seek sets clip-local time without changing the state' (legal in Playing and Paused). These conflict: a seek-past-duration from the Playing state both 'does not change state' (ANIM-4) and 'is treated as Ended' (ANIM-6, a state change). Also undefined: does seeking back to t<duration from Ended re-arm to Playing/Paused, and does that seek re-emit anim_end on the next end? No test covers seek-out-of-Ended.  → FIX: State precedence explicitly: seek clamps t to [0,duration]; if the clamp lands exactly at duration on a non-looping clip the instance enters Ended and emits anim_end once (dedupe: anim_end fires only on the transition into Ended, not on every tick or repeated seek-to-end); seeking to t<duration from Ended re-arms (Playing if it was Playing, Paused if Paused) and clears the anim_end-sent latch. Add a test: seek to duration (Ended, one anim_end) -> seek to 0.0 (re-armed, no anim_end) -> advance to end again (Ended, anim_end fires again).
- [medium] ANIM-1: doc_changes mutates §17 phase 4 and §6.5, but the test_notes for the core determinism guarantee are partly non-asserting via the PTY seam. Test (4) 'No call to Instant::now() in playback path' is asserted by 'constructing scene with mock clock and never touching real time; pose only changes when mock clock advances' — that is a behavioral proxy, not an assertion of internal state, and it cannot actually prove the absence of an Instant::now() call (a stray real-clock read in a branch not exercised would pass). This is exactly the hand-wavy test_note the review brief flags.  → FIX: Replace test (4) with a state-asserting test: inject mock clock, send play, do NOT advance the clock, call tick() N times, and assert via tgp_scene() that clip-local time and world matrix are byte-identical across all N ticks (pose strictly a function of mock-clock value). Optionally add a compile/lint-level guard (deny Instant::now in the playback module) as a non-test backstop, but do not count it as the unit test. The PTY-bytes-in assertion is: bytes(play) -> assert t==0 stable across ticks with frozen clock.
- [medium] ANIM-9: Conflicts with SCEN-10's resolution that the renderer gate switches from numeric revision equality to per-node/per-viewport dirty flags. ANIM-9's rationale still argues against 'a strict revision-equality renderer' and frames the design as avoiding a per-frame scene_revision bump to not defeat re-upload — but if SCEN-10 lands (u64 revision + dirty-flag-driven gate, eliminating the wrap hole), the 'discrete transitions bump scene_revision once' clause in ANIM-9 is partly vestigial and the two clusters must agree on what scene_revision even drives. Also ANIM-9 says discrete play/pause/seek/stop 'bump scene_revision once' but per 6.5 today scene_revision bump also triggers cell re-emit; a pause that only freezes a pose should arguably not re-emit cells.  → FIX: Align ANIM-9 with SCEN-10: state that per-frame playback sets per-node + per-viewport dirty flags and the renderer re-renders off those flags (not revision equality); scene_revision (u64 per SCEN-10) is bumped once only on discrete transitions that change scene STRUCTURE/ownership (play start, stop). Clarify whether pause/seek (which change owned-channel set? no) bump scene_revision at all — recommend pause/seek bump only the per-node dirty flag, not scene_revision, since no structural change occurs. Add a cross-ref note to SCEN-7/SCEN-10.
- [low] ANIM-5: camera_busy interacts with EXPL/F4 but the resolution does not address the auto_spin explore option, which is itself terminal-side camera animation. ANIM-5 forbids anim;play on an explore-enabled viewport's camera and auto-stops clip playback when explore is enabled. But ExploreOpts.auto_spin (12.3) is ALSO terminal-side camera motion driven by the same clock; if both auto_spin and a clip could target the camera, the 'at most one terminal-side controller' rule needs to name auto_spin as the controller that wins when explore is enabled. As written, anim;play->camera_busy because explore is enabled, but it is left implicit that auto_spin (not the user drag) is what owns the transform.  → FIX: State explicitly that 'enabling explore' includes the auto_spin sub-mode, and that the explore controller (orbit/pan/zoom/auto_spin collectively) is THE single camera controller for that viewport; anim;play on that camera is camera_busy regardless of which explore sub-features are active. Add a test: vp with explore{auto_spin} (no orbit) + anim;play;node=cam -> camera_busy.
- [medium] ANIM-8: Deterministic ClipId allocation claim is unverifiable and over-promised. ANIM-8 says 'ClipId allocation is deterministic given asset content + a per-token monotonic counter so tests can predict the mapping (or read it from the ack).' A per-token monotonic counter makes ClipIds depend on the ORDER and COUNT of all prior asset.add calls in the session, so 'given asset content' alone does NOT predict the ClipId — only the full session history does. The parenthetical 'or read it from the ack' is the only reliable handle. The 'deterministic given asset content' phrasing will mislead test authors. Also, the ack frame format 'tgp;a;txn=1;asset=7;clips=0:100,1:101' is a control frame carrying a structured map but 8.5 lists 'a' as 'ack (optional)' with no clips= field defined.  → FIX: Drop 'deterministic given asset content'; state ClipIds are allocated from a per-token monotonic counter (deterministic given the full ordered frame sequence the test sends) and the canonical handle source is the clips= map in the a ack. Make the test read ClipIds from the ack rather than hardcoding 100/101. Ensure the clips= field is added to the 8.5 'a' ack schema (the doc_changes mention it for 8.5 — good, but 8.5's table row must gain the field, and confirm 'a' acks are gated on the same opt-in as errors/events per CAPS-7).
- [low] ANIM-3 / ANIM-8: Two different anim_stop reasons for asset/node removal with overlapping coverage and unspecified emission when both apply. ANIM-3 emits anim_stop;reason=node_removed; ANIM-8 emits anim_stop;reason=asset_removed; ANIM-5 emits reason=explore_enabled; ANIM-6 emits anim_end. Removing the owning asset (ANIM-8) of a clip that animates nodes which are then implicitly... no, asset removal does not remove nodes. But if an app does node.remove on the animated node AND removes the asset in the same write, ordering decides which reason fires; this is unspecified. Minor, but the reason enum and dedupe (one event per stopped instance, not one per reason) should be pinned.  → FIX: Centralize the anim_stop reason enum {node_removed, asset_removed, explore_enabled} (plus anim_end as a distinct ev) in one place (§12.5 ev/reason enum) and state exactly one anim_stop event is emitted per stopped instance, with reason = the proximate cause of the stop in receive order. Note these are subject to the same subscription + token-routing (F1) gating.
- [low] ANIM-11: Relative channel-target resolution is normatively asserted but the import-time capture of 'relative path/index along the node hierarchy' is hand-wavy about glTF reality: glTF animation channels target node INDICES in the glTF node array, not names, and the clip's own root may not be a single node (glTF scenes can have multiple roots). 'as captured at import' does not say HOW the relative path is derived when the imported subtree root is ambiguous, nor how it maps to TGP's app-assigned STRING node ids built by separate node.upsert ops. The robot-arm example builds nodes via upsert with app string ids that have NO necessary correspondence to the GLB's internal node names/indices.  → FIX: Pin the import mapping rule: when a clip is auto-registered from a GLB (ANIM-8), capture each channel's target as the relative path of glTF-node-NAMES (or index path) from the GLB's animation/scene root. At play time, resolve by matching those names/indices against the live subtree under node= using TGP node ids — but state the matching key explicitly (TGP node id string must equal the glTF node name? or a separate name field on node.upsert?). The robot-arm case only works if upsert ids match GLB node names; make that requirement normative or add a name= field. Add a test that the matching key is well-defined.

**Cross-cluster:**
- ERROR-CODE ENUM (SEC CAPS-5): ANIM introduces 8 new error codes (node_busy, forbidden, camera_busy, not_playing, unknown_clip, unknown_node, no_targets, bad_param). SEC CAPS-5 mandates a CLOSED, VERSIONED code enum and §15.3 in the doc today enumerates only cap_exceeded and parse_error. All 8 ANIM codes MUST be added to that single closed enum during reconciliation; 'forbidden' in particular is an F1-wide code that EXPL (cross-token sub), SCEN (cross-token upsert), and VIEW (cross-token vp retarget) will also need — it should be owned by the SEC/F1 layer, not minted per-cluster. bad_param overlaps SEC CAPS-5's existing taxonomy intent; confirm naming (bad_param vs the F3 inline-over-cap code).
- F1 TOKEN ROUTING (LIFE-6, EXPL-2): ANIM-2/3/4/5/8/10 all assert 'forbidden' on cross-token access and route x/e/a only to the owning token. This depends on LIFE-6's 'opt-in app-namespace token before honoring event subscriptions' actually being resolved as the per-node OWNERSHIP token (not just an event-routing token). ANIM assumes the token scopes node ID ownership AND addressing AND event routing; LIFE-6's rec is narrower (only 'event subscriptions'). These must be unified: F1 says addressing is isolated, so SCEN/LIFE must define token-scoped node namespaces, not just token-scoped event delivery. If SCEN-8's id-namespace work does not incorporate the token, ANIM's forbidden checks have nothing to check against.
- EXPLORE EXCLUSIVITY (EXPL-1, EXPL-7, EXPL-10): ANIM-5's camera_busy/explore-auto-stop interacts with EXPL-1 (raw mutually exclusive with explore+events) and EXPL-7 (camera sync-back). The 'single terminal-side camera controller' invariant must be stated once and shared: explore (incl. auto_spin) OR a clip, never both. EXPL-9 already makes raw and explore mutually exclusive; ANIM-5 adds clip vs explore. Reconcile into one camera-ownership rule in §12.3/§14.
- DIRTY-FLAG RENDER GATE (SCEN-10, SCEN-7): ANIM-9's whole repaint scheme assumes the renderer gates on per-node/per-viewport dirty flags rather than scene_revision numeric equality. SCEN-10 makes exactly that switch (and moves revision to u64). ANIM-9 must adopt SCEN-10's gate model verbatim and stop arguing against 'revision-equality renderer' as if it might persist. SCEN-7 (instance buffers bump scene_revision + per-node instance-dirty) sets the precedent ANIM-9 should mirror for animation dirty.
- CLOCK / ANIMATION LIFETIME (LIFE-7, LIFE-10): ANIM-1's injectable clock interacts with LIFE-7 (animation lifetime tied to subscribing app; cancel on foreground change / never emit while unfocused) and LIFE-10 (track animation phase in resolution-independent terms across resize/device-loss). ANIM-1 stores clip-local time as f64 seconds (resolution-independent — consistent with LIFE-10, good). But LIFE-7 says terminal-side animation is CANCELLED when the foreground process changes / PTY input closes — ANIM-4's state machine has no 'cancelled-by-lifecycle' transition. Add: foreground-change / PTY-close stops all of that token's playback (like ANIM-3 auto-stop) and whether anim_end vs anim_stop;reason=app_gone fires. Coordinate the reason enum with LIFE-7.
- CAPS / ANIM_SPEED_MAX (CAPS-3, CAPS-9): ANIM-6 adds anim_speed_max and ANIM-7 removes feat=skin from the v1 feat set. CAPS-3 freezes the v1 feat token set and CAPS-9 wants §7.1 reply and §15.2 list to be the SAME closed set. So anim_speed_max must be added to the §7.1 caps list (ANIM-6 doc_changes says this — good) AND the feat=skin absence must be reflected in CAPS-3's frozen v1 feat set (note feat=skin reserved-for-phase-4, not omitted-unknown, so apps can distinguish 'not yet' from 'unknown token').
- WIRE BIN-FOLLOW (WIRE-2): ANIM frames are control frames (per 8.5 anim=control) and carry no binary payload, so they are unaffected by the apc_end BinaryFollow back-channel (WIRE-2). Confirm anim never needs enc=/len=. This is consistent — note only that ANIM-10's 'strict receive order across patch/anim/vp/sub frames' must hold even when a preceding patch is a binary (len=N) frame still being drained; ordering is by frame-completion, so an anim frame after an incomplete binary patch must wait for the binary drain (WIRE-9 persisted remaining-count). Cross-check ANIM-10 test (1) single-write patch-then-anim against WIRE-9's multi-read buffering.

**Missed:**
- No resolution for what tgp;q / capability re-query (CAPS-4, CAPS-5) reports about ACTIVE animation state, nor whether anim_speed_max can change on device-loss/headless transition (CAPS-4 makes caps runtime-variable). If anim_speed_max shrinks mid-session, in-flight playback at a now-over-cap speed is undefined.
- No resolution addresses ACK emission gating for anim. ANIM-4/8 say 'optional a acks (when subscribed) echo state' but CAPS-7/SEC CAPS-6 tie error/event opt-in to the completed tgp;q handshake, not a separate 'subscription'. Pin whether anim acks ride the handshake opt-in (like x) or require an explicit sub. As written 'when subscribed' is ambiguous — subscribed to what? sub is per-viewport/node for INPUT events (12.5), not for anim acks.
- Interpolation mode is unspecified. §14 says clips import STEP/LINEAR/CUBICSPLINE interpolation, but no ANIM resolution states which the v1 sampler implements or what happens on CUBICSPLINE (which needs in/out tangents). A test asserting 'world matrix == clip sample at 0.5s' (ANIM-1 test 1, ANIM-6) is undefined without pinning the interpolation. This is core to the testability imperative — the 'exact pose' assertion has no defined oracle without the interpolation rule.
- Quaternion handling for rotation channels is unspecified: glTF rotation channels are quaternions; LINEAR interpolation of quaternions should be SLERP (or normalized-lerp). Without pinning this, 'assert base world matrix == clip sample at 0.5s' is non-deterministic across implementations. Add to ANIM-1 or a new sub-decision.
- No resolution for playback when the animated node is HIDDEN (node.visible=false, 8.6) or its viewport is not rendered (LIFE-1 inline hidden on buffer switch). Does the clip clock keep advancing while the node/viewport is hidden? ANIM-9 keys repaint off animation_deadline; if a viewport is hidden, does animation_deadline still fire and burn CPU? Interacts with LIFE-1/LIFE-7.
- speed precision and clip-local time units: ANIM-1 stores t as f64 seconds and ANIM-6 clamps speed to (0, anim_speed_max], but there is no statement that clip durations are in seconds or how a clip with a very large duration * large speed avoids f64 catastrophic cancellation over a long session (RGP's comment at scene.rs:209-215 notes phase precision degradation). A long-running looping clip needs a defined wrap (t mod duration is stated for loop — good — but the non-loop Ended hold means t can grow unbounded only until duration, so OK; just confirm the loop wrap is applied to t BEFORE sampling so f64 stays bounded).
- No resolution states whether anim;play on a node that is itself an Instanced node (8.6 node.instances) is legal — clips animate per-node TRS, but an Instanced node's transforms live in a per-instance buffer (SCEN-6). Can a clip drive an Instanced node, and if so which instance? Likely should be rejected (bad_param or unsupported) but it is unaddressed; ANIM-2's channel-ownership model assumes scalar TRS channels, not instance buffers.

## SCEN  (consistency_ok=False)

**Issues:**
- [high] SCEN-5: Direct cross-cluster contradiction with VIEW-5/VIEW-6 over dangling camera refs. SCEN-5 makes removing a node that is a viewport's camera/root a HARD atomic txn rejection (x code=bad_ref;detail=viewport:<vp>;camera|root) unless the same txn also fixes the viewport, and routes the dangling ref through the SCEN-4 final-state rule. But VIEW-5's recommendation is an IMPLICIT DEFAULT CAMERA fallback (auto-frame the root subtree) so dangling/removed camera refs degrade gracefully and KEEP RENDERING (only an opt-in diagnostic), and VIEW-6 makes Viewport.camera optional with that same implicit default. The two resolutions are mutually exclusive: SCEN-5 forbids the very state (a viewport whose camera was removed) that VIEW-5/6 say must render with a fallback. Worse, §18.1 step 3 creates vp=2 referencing camera 'cam' BEFORE txn=2 creates 'cam' — under SCEN-5's bad_ref-at-commit rule that documented example is now an error at vp-create time unless camera existence is also deferred, which SCEN-5 only addresses for node.remove, not for vp-create-before-camera.  → FIX: Reconcile with VIEW before finalizing: either (a) adopt VIEW-5/6's implicit default camera so a removed/absent camera degrades to auto-frame (and SCEN-5 then only needs to detach the viewport's camera ref to the default, never reject the txn), or (b) overturn VIEW-5/6 and make dangling camera a hard error everywhere (then VIEW must drop the default-camera fallback and §18.1 vp-before-camera ordering must be forbidden or camera-existence deferred). Pick ONE owner for 'viewport with missing camera' semantics and cross-reference it. Also explicitly state vp-create with a not-yet-existent camera ref (the §18.1 case) — SCEN-5 only covers remove, leaving create-before-camera unresolved.
- [high] SCEN-9: Contradicts WIRE-5 on the same scenario (a second open more=1 chunk stream for an id that already has an in-flight buffer). SCEN-9(c) rejects the NEW stream with x code=chunk_conflict and keeps the prior buffer ('one in-flight buffer per id'). WIRE-5's recommendation is the OPPOSITE: 'second open stream for same id or format mismatch ABORTS THE PRIOR, matching RGP's mid-stream-mismatch drop' — i.e. discard the prior and accept the new. The grounded RGP handler (handler.rs DEFAULT_PENDING_CAP_PER_ID) and WIRE-5 both lean to abort-prior; SCEN-9 silently picks reject-new. Two clusters specify opposite behavior for byte-identical input, so a fuzzer/implementer cannot satisfy both. SCEN-9 also overlaps WIRE-6 (chunk frames carry no txn semantics; only final assembled patch applies atomically) without citing it — SCEN-9 asserts 'asset_revision bumps once at more=0' which is fine, but the chunk-vs-txn atomicity ownership is WIRE-6's, and SCEN-9 should defer to it rather than re-deriving.  → FIX: Align SCEN-9(c) with the WIRE-5 resolution (abort-prior vs reject-new) — they must agree; recommend abort-prior to match RGP's grounded behavior and WIRE-5's rec, OR get WIRE-5 changed to reject-new, but not both. Add explicit cross-references from SCEN-9 to WIRE-5 (stream keying/interleaving) and WIRE-6 (chunk frames are pre-parse buffering, no txn semantics) so the chunk lifecycle has one owner. Note: SCEN-9 also doesn't address what happens to an in-flight buffer when an unrelated patch/txn arrives mid-stream (WIRE-5's 'non-asset.add patch mid-stream' case) — defer to WIRE-5 explicitly.
- [high] SCEN-8: F1 conformance is incomplete on the ROUTING half. F1 requires that 'event reports, error replies, and acks route ONLY to the owning app.' SCEN-8 correctly scopes addressing (ids per token, cross-token => x code=denied) but never says WHERE the x code=denied error itself is routed — to the offending writer's token or the owner's? Routing a 'denied' error to the owner would leak the existence/activity of other tokens (an info-leak / cross-app channel) and spam the owner; routing it to a tokenless/untrusted writer means emitting tgp;x to a process that by definition didn't negotiate error reporting (conflicts with CAPS-6/CAPS-7 which gate x on a completed handshake). This is left vague exactly where F1 is most load-bearing. Also: SCEN-8 says ids are token-scoped but the Scene struct (§6.1) is 'a single retained scene per terminal session' with one nodes:Map<NodeId,Node> and one roots:Set — SCEN-8 never specifies the storage representation of token scoping (compound key (token,id)? per-token submaps? prefix?), so its own test note ('two distinct tokens each upsert id:cam -> two independent nodes in their respective scopes') has no defined &self accessor signature to assert against (accessor must take a token, which the proposed scene model lacks).  → FIX: State the routing of cross-token errors explicitly: a tokenless/untrusted write that targets another token's node must NOT emit to the owner (no info-leak) and should be silently dropped unless the WRITER negotiated its own error channel, in which case x code=denied goes to the writer only. Define the token-scoped storage: change the node accessor to scene.node(token, id) (or a per-token NodeId namespace) and say so in the test note so the PTY-bytes->internal-state assertion is actually expressible. Cross-link to LIFE-6/LIFE-7 (subscription/event routing on the token) and CAPS-6/7 (who is allowed to receive x at all).
- [medium] SCEN-2: Kind-immutability uses x code=kind_conflict for THREE distinct failures (transition, multiple selectors, instanced-node-tint in SCEN-11) — overloaded code with only detail= to disambiguate. CAPS-5 mandates a closed versioned error-code enum where 'codes are stable, detail is free-form/advisory'; if detail is advisory/non-load-bearing per CAPS-5, then an app cannot programmatically distinguish 'you tried to change kind' from 'you sent two selectors' from 'you set a tint on an instanced node' — defeating principle-4 fallback branching. Either these need distinct codes or SCEN must explicitly carve out that for kind_conflict the detail IS load-bearing, contradicting CAPS-5's blanket 'detail advisory'.  → FIX: Either split into stable codes (e.g. kind_conflict / kind_multi_selector / instanced_node_tint) registered in the CAPS-5 enum, or get CAPS-5 to declare a documented closed set of detail tokens for kind_conflict that ARE load-bearing. Cite CAPS-5 and reconcile the 'detail advisory' tension explicitly.
- [medium] SCEN-7: The buffer-grow-without-asset_revision rule is asserted but its test_note does not assert the actual GPU-relevant internal state via the PTY seam. Test (3) 'instances{count:N+1000} (grow) -> asset_revision UNCHANGED, instance_dirty set' only checks the dirty flag and the negative (asset_revision unchanged); it never asserts the CPU-side instance buffer actually now holds N+1000 records (capacity/len growth), which is the load-bearing claim ('renderer grows that node's instance buffer'). Per the testability imperative the assert must hit instance-buffer internal state (count/len), not just a boolean flag. Also SCEN-7 leans on CAPS-10 territory (a rejected over-cap node.instances in a live per-frame stream must retain the last committed buffer to avoid flicker) but never states the retain-last-good behavior; CAPS-10 owns it but SCEN-7's atomic-rejection wording could be read as clearing the node.  → FIX: Strengthen test (3) to assert the &self instance-buffer accessor reports count==N+1000 and the decoded records, not just instance_dirty. Add a cross-reference to CAPS-10: an over-cap node.instances rejects atomically AND retains the previously-committed buffer (no flicker); add a test sending a good instances{} then an over-cap instances{} and asserting the buffer still holds the FIRST (good) records.
- [low] SCEN-3: max_node_depth=256 is advertised and enforced, but SCEN-3 never reconciles with the §15.2 max-nodes session cap (CAPS-9 flags that 'max nodes' is in §15.2 but missing from the §7.1 reply). A 257-deep chain test triggers depth_exceeded, but if max-nodes (session) is, say, 200, the chain would hit the nodes cap first — the precedence between the two caps (which error fires) is unspecified. Minor, but two caps gating the same op need a defined order for deterministic structured errors.  → FIX: State precedence: node-count cap (max_nodes) is checked before/independently of depth; cite CAPS-9 and ensure max_node_depth and max_nodes both appear in the SAME §7.1 reply and §15.2 list (CAPS-9's closed-set requirement). Add a one-line note on which error fires when both would.
- [low] SCEN-11: SCEN-11 introduces a NEW op node.instance_set{id,index,tint} (single-record partial update) and adds it to the §8.5/§8.6 op list. This new op interacts with SCEN-9 chunking, SCEN-7 instance_dirty, and the F1 token scoping (does instance_set respect token ownership of the target node?) but none of those interactions are stated. It also asserts 'tint multiplies exactly like the Mesh node tint (NOT emissive add)' — consistent with MATE-9's sRGB->linear chain, good — but the instance_set tint's sRGB-decode is not re-stated, so an implementer might write a raw RGBA8 record without the decode the chain requires. The instance_set test (2) asserts 'only record 6 changed, xforms untouched' which is good and properly PTY-seam-testable.  → FIX: State that node.instance_set is token-scoped per F1 (cross-token index write => denied, same as upsert), bumps scene_revision + instance_dirty (already implied) but never asset_revision (cite SCEN-7), and that its tint record is stored sRGB-encoded and decoded in the same chain as per-instance tints (cite SCEN-11's own chain / MATE-9). Confirm index>=count fires bad_index BEFORE any per-record write (atomic).
- [low] SCEN-1: SCEN-1 says parent:null re-roots 'retaining its children and animation phase', explicitly invoking 'animation phase'. But under F2 (clean break, no inherited spin) and the ANIM cluster (ANIM-2: a clip exclusively owns a playing node's TRS; app upsert => x node_busy), the concept of a node-held 'animation phase' that survives a re-root is an RGP-ism (RgpPlacement.animation_phase_rad, scene.rs:63). TGP animation is clip-driven via an injectable clock (ANIM-1), not a per-node phase float. Citing 'animation phase' as a preserved field risks importing the RGP model F2 says to leave behind, and creates a latent conflict with ANIM-2 (if the node is clip-owned, re-rooting it mid-clip needs the node_busy/clip-ownership rule, not a phase-preservation rule).  → FIX: Replace 'animation phase' with the F2/ANIM-correct notion: re-rooting preserves the node's clip binding / playback state per the ANIM cluster, not an RGP-style scalar phase. Cross-reference ANIM-1/ANIM-2 and confirm a parent:null on a clip-owned node is allowed (transform structure change) vs a TRS write which would be node_busy. Drop the RGP phase framing to stay F2-clean.

**Cross-cluster:**
- SCEN-5 <-> VIEW-5/VIEW-6: HARD conflict. SCEN-5 rejects a txn that leaves a viewport's camera/root dangling (x bad_ref); VIEW-5/6 recommend an implicit default camera fallback that keeps rendering. Must be reconciled — they cannot both ship. Also SCEN-5 only covers node.remove, not the §18.1 vp-create-before-camera ordering case VIEW-5 raises.
- SCEN-9(c) <-> WIRE-5: HARD conflict on a second concurrent in-flight chunk stream for one id. SCEN-9 rejects the new (chunk_conflict, keep prior); WIRE-5 aborts the prior (matching grounded RGP handler.rs). Opposite behaviors for identical bytes.
- SCEN-9 <-> WIRE-6: overlap (chunk frames carry no txn semantics; asset_revision bumps once at more=0). Consistent in spirit but SCEN-9 re-derives instead of deferring to WIRE-6 as owner; add explicit cross-reference to avoid drift.
- SCEN-8 <-> LIFE-6/LIFE-7 + CAPS-6/CAPS-7: F1 token scoping spans clusters. SCEN-8 owns id-addressing isolation; LIFE-6/7 own event/subscription routing per token and lifetime on process change; CAPS-6/7 own who may receive tgp;x at all. SCEN-8's cross-token 'x code=denied' routing is undefined and must be settled jointly (recommend: never route to owner; only to writer if writer negotiated errors).
- SCEN-2 & SCEN-11 <-> CAPS-5: kind_conflict (and instanced_node_tint, bad_index, bad_layout, dup_asset, chunk_conflict, depth_exceeded, null_not_allowed, denied) are all NEW error codes the SCEN resolutions invent. CAPS-5 mandates a single closed versioned code enum with advisory detail. Every code SCEN introduces must be registered in that enum, and any case where SCEN relies on detail being load-bearing (kind_conflict's multiple_selectors vs transition) violates CAPS-5's 'detail advisory' unless explicitly carved out.
- SCEN-3 <-> CAPS-9: max_node_depth (new) and max_nodes (already in §15.2 but missing from §7.1 reply) must both land in the SAME closed caps set CAPS-9 requires, with defined precedence when both would fire.
- SCEN-7 <-> CAPS-10: over-cap node.instances atomic rejection must retain the last committed buffer (CAPS-10) to avoid per-frame flicker; SCEN-7 doesn't state retain-last-good and its atomic-reject wording is ambiguous about whether the node is left intact.
- SCEN-6/SCEN-11 <-> MATE-9: instance tints (RGBA8 sRGB) and the multiply chain depend on MATE-9's 'wire colors sRGB, linearize on input' resolution. SCEN-11 states the decode for tints but SCEN-6 just says 'RGBA8 sRGB (matches color=srgb cap)' without restating the decode-before-multiply; keep aligned with MATE-9 so default-material and instance paths agree.
- SCEN-1 <-> ANIM-1/ANIM-2: SCEN-1's 'preserve animation phase on re-root' imports an RGP-style per-node phase that F2 discards and ANIM-2 (clip exclusively owns TRS) supersedes. Re-root semantics for a clip-owned node need the ANIM ownership rule, not phase preservation.

**Missed:**
- No SCEN resolution addresses LIFE-2 (RIS = full scene teardown). RIS must drop all nodes/roots/in-flight reassembly buffers (SCEN-9) and reset both revisions (SCEN-10). SCEN-9 mentions a reassembly cap/timeout reclaiming open buffers but never the RIS teardown path; SCEN-10's monotonic counters need a defined reset-to-0-on-RIS or the renderer's seen-revision gate (lib.rs:958-965, which inits to u32::MAX) mis-syncs after teardown. The SCEN cluster owns the scene-state half of LIFE-2 and is silent.
- node.visible is in the §8.6 op list and §6.5 dirty table ('transform/visibility') but NO SCEN decision resolves its semantics: is visible:false on a Group cascade-hiding the subtree (like SCEN-5 cascade-delete) or node-local only? Does hiding a node that is a viewport root render blank? Does it interact with picking (hidden nodes excluded from the pick target — EXPL-4/5 assume so)? This is an unresolved op the cluster should have pinned alongside SCEN-5.
- SCEN-6 forbids NaN/inf in instance matrices (good) but no SCEN decision applies the same NaN/inf rejection to node.upsert TRS (the trs field in SCEN-1). A NaN in a node's quaternion/scale propagates through the world-matrix chain (SCEN-3's lazy walk) and poisons every descendant — the same untrusted-bytes hazard SCEN-6 guards for instances is wide open for plain nodes. SCEN-1 should reject NaN/inf in trs sub-fields with parse_error, symmetric with SCEN-6.
- CAPS-4 (two-phase VRAM accounting: a txn whose Nth asset.add busts max_vram_mb must reject the whole txn and free assets staged 1..N-1) directly interacts with SCEN-4 (atomic final-state resolution) and SCEN-7 (instance buffer growth counts VRAM), but no SCEN decision states that the staged-copy validation in SCEN-3/SCEN-4 also computes and gates the net VRAM delta. The SCEN atomic-commit pipeline is the natural enforcement locus and is silent on it.
- SCEN-2 says changing kind requires remove+re-add and 'cascades children — the app must rebuild them' (via SCEN-5 cascade). But SCEN-5 offers reparent:'root' to PRESERVE children. SCEN-2 never tells the app to use reparent:'root' to keep the subtree across a kind-change, so following SCEN-2 literally destroys a subtree that SCEN-5 could have preserved — a usability gap between the two same-cluster decisions.
- No decision addresses the order of validation passes at commit when MULTIPLE SCEN checks would fail in one txn (e.g. a txn that has both a kind_conflict (SCEN-2) and a cycle (SCEN-3) and a bad_ref (SCEN-4)). Each decision says 'cite the offending op index' and 'reject atomically', but with several independent failures the WHICH-error-is-reported order is unspecified, making the structured x reply nondeterministic across implementations — exactly the determinism the test imperative needs. Define a fixed validation order (e.g. parse/null -> kind -> ref-resolve -> cycle/depth -> cap).

## CAPS  (consistency_ok=False)

**Issues:**
- [high] CAPS-9: The enc=bin probe->ack handshake is unimplementable as written. Design doc §8.5 (term.rs taxonomy line 356) defines `a` (ack) as 'optional, if app subscribed' — acks are NOT always-on for a committed txn. The resolution requires an app to 'send a minimal len=N binary frame ... and expect a tgp;a ack for that txn', but never says how the probe arms acks for an app that has not subscribed. As written, a conformant terminal could legitimately send no ack to the probe (because the app did not subscribe), making 'absence of ack => transport mangled' a false-positive downgrade trigger. The test_note ('acks on') hand-waves this with an undefined 'acks on' state.  → FIX: Either (a) make tgp;a unconditionally emitted for any committed txn carrying an explicit ack-request flag (e.g. txn with ack=1), and have the probe set that flag; or (b) make a successful binary-frame commit ALWAYS produce either a tgp;a or a tgp;x for a handshaked token (so the app gets a deterministic positive OR negative signal), and define ack arming precisely. Update the test_note to assert via &self that the probe txn produced exactly one queued tgp;a (or tgp;x) and define the byte that requested it.
- [medium] CAPS-9: The new error variant `code=parse_error;detail=binary_decode` is introduced without reconciling it against the closed, versioned error-code enum that SEC mandates (SEC/CAPS-5 in SEC.md: 'Pin a closed, versioned code enum (codes are stable, detail is free-form/advisory)'). The resolution adds a detail string but the SEC enum lists `truncated`, `cap_exceeded`, `parse_error`, `bad_ref`, `dup_id`, `unknown_op`, `unsupported`, `vram` — a transport-corruption case may warrant a distinct stable CODE, not just an advisory detail. Per SEC, detail is explicitly non-load-bearing, so apps MUST NOT branch downgrade logic on detail=binary_decode — yet this resolution's downgrade path depends on distinguishing transport mangling from ordinary parse errors.  → FIX: Coordinate with SEC: add a stable code for transport/decode corruption to the closed enum (e.g. code=bad_enc or code=truncated) so the app can branch on the CODE (load-bearing) rather than the advisory detail. State that detail=binary_decode is advisory only and the downgrade decision keys off the stable code.
- [high] CAPS-2: Internal contradiction on the buffering/ordering mechanism. The decision says the terminal 'enqueues the tgp;r reply BEFORE the DA1 reply even if DA1 was parsed first (special-cased flush ordering)' and the rationale says it can 'buffer the DA1 reply until the same batch's tgp;r is enqueued'. But pty_replies (term.rs:551) is a Vec extended in dispatch order at the moment each frame is parsed; DA1 is handled by the VTE/CSI path (term.rs ~2088) while tgp;q is handled in apc_end — these are distinct dispatch points reached at different times within advance(). To enqueue tgp;r before an already-parsed DA1, the terminal must either (a) defer DA1's enqueue (hold it aside until end of batch), or (b) post-sort pty_replies before drain. The resolution asserts the invariant but specifies neither mechanism, and 'buffer the DA1 reply' contradicts the plain FIFO-in-parse-order claim in clause (a) of the same decision.  → FIX: Pick and specify ONE mechanism: e.g. 'within an advance() batch, the terminal defers emitting any DA1 reply into pty_replies until the batch completes, so any tgp;r parsed later in the same batch is enqueued first' OR 'a final reorder pass moves a same-batch tgp;r ahead of DA1'. Then the test must assert the chosen mechanism's internal state (e.g. a pending-DA1 holding slot), not just final byte order, to pin it deterministically.
- [medium] CAPS-2: Cross-advance() batching is undefined, undermining the guarantee. The invariant is scoped to 'a single advance() batch'. But PTY bytes arrive in arbitrary chunks: an app that emits `ESC[c` and `tgp;q` may have them split across two advance() calls (DA1 fully parsed and enqueued in call 1, tgp;q in call 2). Then pure FIFO puts DA1 first and the terminal cannot retroactively reorder — reintroducing exactly the CAPS-1 hang/false-absence the decision claims to eliminate, with no app-visible rule for when the guarantee holds.  → FIX: State the boundary explicitly: the terminal's reorder guarantee holds ONLY within one advance() batch; therefore the app MUST emit DA1 and tgp;q contiguously (single write) AND the normative app rule should also recommend sending tgp;q before DA1 as belt-and-suspenders so FIFO yields the right order even across a split. Add a test feeding the two queries in SEPARATE advance() calls (DA1 first) and assert/define the resulting order.
- [medium] CAPS-8: Two test_notes assert internal state that is not reachable through the PTY seam for a v1-only build, violating the testability imperative. (1) 'a non-TGP build / disabled-TGP path: send tgp;q;v=1, assert NO tgp;r queued' — there is no described PTY-bytes-in way to put the same Term into a non-TGP state; this needs a build-cfg or a &mut disable hook, which the resolution does not define. (2) 'tgp;q;v=1 with term_min=2 -> assert tgp;r has vmin=2' assumes a terminal whose min supported version is 2, but the protocol under design is v1 (doc fixes v1; the v=2 in examples is only the APP's max). For a v1 terminal term_min=term_max=1, so the 'present-but-incompatible (app too old)' branch and the CAPS-6 clamp-down (v=5) tests are not exercisable against the actual implementation — they require a fictitious multi-version/min=2 terminal.  → FIX: Define the test seam explicitly: add an injectable supported-version range on Term (e.g. Term::with_version_range(vmin,vmax) or a &mut test hook) so the clamp-down and too-old branches are reachable deterministically, and an injectable TGP-enabled flag so the 'absent' branch is reachable. State that for the shipping v1 build vmin=v=1 and that the multi-version tests run against the injected range. Same fix applies to CAPS-6's v=5 and term_min=2 test_notes.
- [low] CAPS-6: Clause (a) says a v=-omitted tgp;q is 'STILL answered with a tgp;r carrying the terminal's supported range ... and NO negotiated session' and 'MAY additionally emit a tgp;x parse_error'. This collides with CAPS-7, which makes a COMPLETED handshake the error-arming opt-in: a v=-less query that establishes NO session is, by CAPS-7, un-handshaked and therefore must NOT receive tgp;x (silent-drop default). So clause (a)'s optional tgp;x parse_error contradicts CAPS-7's rule that un-handshaked/tokenless writers get no structured errors.  → FIX: Reconcile: either (i) emit ONLY the tgp;r-with-range for a v=-less query and never a tgp;x (since no session/token exists to route it to, per CAPS-7), or (ii) carve an explicit exception in CAPS-7 that handshake-attempt parse errors on the q/r channel itself are exempt from the handshake-arming rule. State which, and make the CAPS-6 test_note's 'if error path on' precondition concrete rather than conditional.
- [medium] CAPS-4: The unsolicited caps push is specified as routed 'to every app whose session token completed a handshake' but the routing target through the PTY seam is undefined and likely untestable as written. pty_replies is a single shared Vec drained by the binary; there is no per-token reply queue in the grounded code. The test_note asserts 'a second token T2's reply queue is empty', which presumes per-token queues that do not exist in term.rs (single pty_replies). Without specifying how a token-scoped reply is addressed on a single shared writeback FIFO (an in-band token field in the tgp;r? a routing tag the host process demuxes?), 'routed only to T' is not assertable internal state.  → FIX: Specify the addressing concretely: e.g. each token-scoped reply carries an in-band `tok=<token>` field in the tgp;r/tgp;x so the host can demux on the shared FIFO, OR define per-token reply buffers in Term and update the grounding. Then the test asserts the queued tgp;r bytes contain tok=T and not tok=T2 (single-queue model) or asserts T2's buffer empty (multi-queue model) — but the model must be pinned first. This routing model is shared by CAPS-7 (error routing) and must be defined once cluster-wide.
- [low] CAPS-4: The 'material capability transition' trigger list (device loss/restore, headless/SSH transition, resize changing max viable inline cells) is partly hand-wavy and not pinned to assertable internal state. 'a resize that changes max viable inline cells' overlaps F3's max_inline_cols/max_inline_rows and VIEW-10's max viewport px, but the resolution does not say WHICH cap fields are in the push or what numeric threshold counts as 'material' (any change? a change crossing a budget?). The test relies on a term.simulate_caps_change(...) hook that is invented here without definition.  → FIX: Enumerate exactly which cap fields trigger a push (e.g. max_vram_mb, max_inline_cols/rows, max_verts) and define 'material' as 'any monotone-decrease or any change to an advertised numeric cap'. Define the test hook signature (simulate_caps_change(field, new_value)) and assert the pushed tgp;r reflects the new field value with caps_gen incremented by exactly 1.
- [low] CAPS-5: Clause (b)/(d) claim control frames (q/r/e/x/a/vp/sub/anim) 'are never read in binary-length mode — only p and chunked asset frames carry len=N'. But §8.5 marks `vp` as 'control/binary' (viewport create/update MAY be a binary frame), and §8.7 chunking applies to assets. Asserting vp is never binary contradicts the taxonomy and could wrongly make a binary vp frame's payload APC-scanned (corruption) or wrongly treat its bytes as control. The 'q always parseable' guarantee leaks if vp can also be binary.  → FIX: Reconcile with §8.5: either restate that vp uses len=N binary mode when it carries bulk data (so the consume-exactly-N rule and the 'no tgp;q inside a binary frame' rule extend to binary vp), or change §8.5 to make vp control-only. Update the CAPS-5 list of binary-capable frame types to match whichever is chosen.
- [low] CAPS-3: The frozen v1 feat set is tied to v= ('new tokens introduced only by bumping the negotiated version'), but the shipping protocol is v1 and the doc's own §7.2 says 'unknown ops/fields inside a patch are skipped' and a negotiated version 'gates whole feature sets' while field-skipping 'handles minor additions'. Gating ALL new tokens behind a version bump is stricter than §7.2 and conflicts with the forward-compat 'ignore unknown feat tokens' rule in the same decision (clause c) — if apps must ignore unknown tokens anyway, a terminal COULD advertise a new token within v1 without breaking apps, so the hard 'only by bumping v=' rule is over-constrained and internally redundant with ignore-unknown.  → FIX: Soften clause (d) to match §7.2: additive feat tokens MAY appear within a version (apps ignore unknown ones per clause c); only tokens that change EXISTING semantics or imply new mandatory behavior require a version bump. This keeps the implication closure (clause b) as the real invariant while not contradicting the ignore-unknown forward-compat rule.

**Cross-cluster:**
- CAPS-9 vs SEC (SEC-5 / error-code enum): CAPS-9 introduces code=parse_error;detail=binary_decode and bases an app downgrade decision on it, but SEC pins a CLOSED versioned code enum where detail is explicitly non-load-bearing/advisory. Downgrade logic must key off a stable CODE, not detail. SEC's enum must gain a stable code for transport/binary corruption (e.g. bad_enc or truncated). Resolve jointly.
- CAPS-9 vs WIRE (WIRE-3 len authoritative; decode-mismatch->x parse_error+resync, and WIRE-7 enc=bin enters raw-read): CAPS-9's 'binary frame fails to decode -> tgp;x' must be the SAME error path WIRE-3 already defines (decode-mismatch -> x parse_error + resync to Ground), and CAPS-9's enc=bin probe must respect WIRE-7's rule that only enc=bin enters raw-read. Ensure CAPS-9 references WIRE-3/WIRE-7 rather than inventing a parallel failure mode.
- CAPS-4 and CAPS-7 vs F1 / LIFE-6 / SEC-1: token-scoped reply ROUTING (unsolicited caps push, error replies, acks) is asserted but the grounded code has a single shared pty_replies Vec with no per-token queue. F1 (per-app token) + LIFE-6 (opt-in app-namespace token before honoring subscriptions) + SEC-1 (txn in text header) imply a routing/addressing model that must be defined ONCE cluster-wide: how a token-scoped reply is addressed on a single shared writeback FIFO (recommend an in-band tok=<token> field on r/x/a/e frames so the host demuxes). Without it, 'routed only to owning token' is neither implementable nor assertable.
- CAPS-4 vs VIEW (F3 max_inline_cols/rows, VIEW-10 max viewport px): the 'resize changes max viable inline cells' push trigger overlaps F3's advertised inline cell caps and VIEW-10's max viewport px cap. The set of cap fields that can change at runtime and trigger a push must be consistent with the cap fields VIEW defines. Coordinate the field list so a resize-driven inline-cap change and a viewport-px-cap change use the same caps_gen-bumped push.
- CAPS-1/CAPS-2 vs WIRE (WIRE-1 return-to-Ground / writeback ordering on the shared channel): the tgp;r-before-DA1 ordering invariant lives on the same pty_replies FIFO that WIRE governs for binary-frame replies. The deferred-DA1-or-reorder mechanism (CAPS-2 fix) must not conflict with WIRE-1's return-to-Ground reply emission. Confirm the chosen ordering mechanism is compatible with how WIRE flushes parse-error/resync replies mid-stream.
- CAPS-5/CAPS-9 binary-mode frame-type list vs WIRE/SCEN: CAPS-5 claims only p and chunked asset frames use len=N, but §8.5 marks vp as control/binary. WIRE owns the binary-length read state; the authoritative list of binary-capable frame types should be defined in WIRE and merely referenced by CAPS to avoid divergence.

**Missed:**
- No resolution addresses the DA1 reply BYTES the terminal actually emits. Grounding/term.rs shows DA1 replies like \x1b[?62;4;22c (term.rs:2088). CAPS-1/CAPS-2 tests assert ordering against an index of '\x1b[?' but never pin that TGP support is (or is not) advertised IN the DA1 reply itself. Decide whether DA1's attribute list gains a TGP marker; if not, state explicitly that DA1 says nothing about TGP (parallel to the CAPS-8/CAPS-10 RGP-independence rule) so apps don't mine DA1 for TGP presence.
- caps_gen wrap/overflow and reset semantics are unspecified. CAPS-4 mandates a 'monotonically increasing caps_gen' but never says its width, what happens on overflow, or whether it resets on RIS/full teardown (LIFE-2 RIS = full scene teardown). An app that cached caps_gen across a RIS could see it reset and mis-order. Specify width (e.g. u32/u64), wrap behavior, and that RIS resets caps_gen to a defined value, with a PTY-in test (handshake -> RIS -> re-handshake -> assert caps_gen behavior).
- Handshake/token establishment race is unaddressed: CAPS-7 says a completed handshake arms errors and CAPS-4 says pushes go to handshaked tokens, but nothing defines what happens to patches/ops that arrive AFTER tgp;q but BEFORE the app has observed tgp;r (the handshake is one round trip but the app keeps writing). Is the session 'negotiated' the instant the terminal queues tgp;r, or only after the app acts? Pin the terminal-side rule (recommend: armed the moment tgp;r is enqueued) and add a PTY-in test: tgp;q then immediately a malformed tgp;p in the same advance() -> assert tgp;x IS produced (session already armed).
- No resolution defines how a token is actually carried on the wire in q/p/x/e/a frames. Every CAPS resolution leans on 'the owning token' for routing (CAPS-4/7) and addressing (per F1), but the wire syntax for the token field (e.g. tok= in tgp;q and echoed in every subsequent frame) is never specified. This is the load-bearing mechanism for F1 across the whole cluster and must be pinned with a PTY-in test (two tokens, assert each token's frames are accepted/rejected/routed independently).
- CAPS-6 clause (b) negotiated = min(app_max, term_max) does not state what the NEGOTIATED session version controls downstream (which feat tokens are valid, which ops). CAPS-3 ties the feat vocabulary to v=, so the negotiated version must gate the advertised feat set, but neither resolution states that the tgp;r's feat= list MUST reflect the NEGOTIATED version (not term_max). Add: the feat= set in tgp;r is the set valid at the negotiated version, with a PTY-in test asserting a v=1-negotiated reply omits any v=2-only token.

## LIFE  (consistency_ok=False)

**Issues:**
- [high] LIFE-6: Per-token EVENT ROUTING is physically unimplementable as written and directly contradicts the grounding. The grounding says the writeback channel is a SINGLE FIFO (queue_reply -> pty_replies, the same path as DA1), and EXPL-11 confirms 'one FIFO back-channel'. A PTY has exactly ONE input stream delivered to the current foreground process; the terminal cannot deliver bytes selectively to token A's process and withhold them from token B when both share the same PTY master/slave. F1 says 'event reports... route ONLY to the owning app', but on a shared PTY with multiple processes (shell + curl|cat + backgrounded job) there is no OS mechanism to do this. The resolution invents a test accessor `pty_replies_for(token) -> &[u8]` that implies multiple physical reply channels, which the grounding contradicts.  → FIX: Make explicit that token isolation of ROUTING is enforced at the WRITE/ADDRESSING boundary, not the transport: the terminal only emits a token's events while that token's process is the foreground PTY input owner AND focused (per LIFE-7), so at any instant at most one token is eligible to receive on the single FIFO. State that 'route only to owning app' is realized as 'suppress emission unless the owning token is the live foreground reader' rather than per-token demultiplexing. Re-spec the accessor as a single `pty_replies()` FIFO plus a CPU-side `routed_token_of(reply) -> Token` predicate / `eligible_to_emit(token) -> bool` so the test asserts that B's events are NOT enqueued (not that they go to a separate buffer). Cross-reference LIFE-7 as the enforcement mechanism.
- [medium] LIFE-2: Two internal/grounding contradictions. (1) CSI 3J: the resolution says '3J clears scrollback only and follows the inline-anchor eviction rule... never removes pinned viewports or assets', but grounded erase_display mode 3 in term.rs is handled identically to 2J ('3 = also scrollback, which we treat the same in M3') — it calls clear_visible AND image_grid.clear(), i.e. it clears the VISIBLE grid and images too, not 'scrollback only'. The decision text mischaracterizes current 3J behavior. (2) The decision says RIS 'invalidates all session/namespace tokens', but it also says RIS 'emits NO TGP error/event/ack to any app' — yet test_note (3) asserts an old-token op after RIS is 'rejected (token invalid)'. A rejection that emits no tgp;x is indistinguishable from a silent drop, which violates Principle 3 (structured errors not silent drops) for a participating app.  → FIX: (1) Either align with code (3J clears visible+scrollback+images like 2J, so an inline viewport with cells only in the visible grid/scrollback is destroyed and pinned persists — same as 2J) or explicitly flag that this resolution CHANGES 3J semantics from current code and say so. Do not assert '3J clears scrollback only'. (2) Clarify: RIS itself emits nothing, but a POST-RIS op carrying a now-invalid token SHOULD emit tgp;x;code=token_denied/token_invalid to that op's sender (it is a new addressed op, not the reset). Reconcile with LIFE-6's token_denied code. Update test_note (3) to assert the queued tgp;x.
- [medium] LIFE-7: Vague trigger and untestable seam for 'foreground process / PTY input owner changes away from the token owner'. The terminal-side code (term.rs) operates on a byte stream via Parser::advance; it has no inherent notion of 'foreground process group' — that lives in the OS tty/job-control layer, not in Term. The resolution names this trigger as load-bearing for cancelling subs/explore/anim but gives no injectable seam, so the test_note 'Signal foreground-owner change away from A' asserts against state the testability mandate's Parser::advance + &self accessor model cannot reach without a defined hook. This is the same class of hand-wavy test_note the review is asked to catch.  → FIX: Define a concrete injectable signal on Term (e.g. `Term::set_foreground_owner(Option<Token>)` and `Term::on_pty_input_eof()`), parallel to the injectable clock and focus signal, so the cancellation logic is a pure CPU state transition assertable via `subscription_count(token)`. State that the real wiring of foreground-owner detection to this hook is an integration concern outside Parser::advance, but the cancellation SEMANTICS are unit-tested through the hook. Without naming the seam the resolution is not testable per the imperative.
- [medium] LIFE-1: The default ScreenAffinity is self-contradictory / under-specified for inline. The decision says ScreenAffinity default = 'buffer active at create time', and inline is 'implicitly Primary-or-Alt = their create-time buffer'. But the enum is {Primary, Alt, Both} with no 'current/create-time' variant, so 'default = create-time buffer' must resolve to a concrete Primary or Alt at creation. The prose 'Primary-or-Alt' reads like a third state. Also, F3 makes placeholder cells authoritative and inline viewports live in the grid that owns those cells; binding inline affinity to a separate stored field is redundant with (and could disagree with) which grid actually holds the placeholder cells — a dual-source-of-truth echo of VIEW-12 which F3 explicitly resolved against storing position separately.  → FIX: State that affinity is RESOLVED to a concrete Primary or Alt at create time (no 'current' variant persisted) for the enum to be well-formed. For inline specifically, do not store a separate affinity field at all: inline visibility follows the grid that owns its placeholder cells (F3 authority) — primary-bound placeholder cells are simply absent from the alt grid, so 'hidden on alt enter' falls out of the stash/restore of the grid, not a parallel flag. Reserve the screen_affinity field for PINNED viewports only (which are not cell-bound). This removes the dual-source-of-truth and aligns with the kitty image stash model the rationale cites.
- [low] LIFE-11: The stall/timeout guard's owner attribution is unspecified for the exact case it exists to handle. The guard emits tgp;x;code=timeout to 'the owning token (if it opted into errors)', but a stalled BINARY frame is precisely one whose CBOR header was never fully parsed (per CAPS-1 the txn/owner may be unknown until decode), and an anonymous/tokenless writer (LIFE-6) has no error opt-in. So for the hostile-stall case the resolution most cares about, there is likely no owner to notify and the error is dropped — leaving the guard's structured-error claim partly hollow.  → FIX: Specify that timeout/truncated errors require the owning token to be derivable from the TEXT header (hoist txn AND token into the text header per CAPS-1/SEC-1 so they survive a payload-level abort); if the writer is tokenless/anonymous, free buffers SILENTLY (no addressable recipient) — and say so explicitly rather than implying an error is always emitted. Cross-reference CAPS-1's 'txn in the text header' decision as a hard dependency.
- [low] LIFE-3: Eviction granularity vs the destroyed-event routing interacts with LIFE-7 unaddressed. LIFE-3 emits tgp;e;ev=destroyed to the owning token, but LIFE-7 says a token's subscriptions are cancelled when its process is no longer the foreground owner — the common case for a long-running background app whose inline viewports age out of scrollback. So the destroyed event the app subscribed to for re-placement will frequently be suppressed by LIFE-7 exactly when eviction happens (app scrolled away / not foreground). The two P1 resolutions are individually coherent but jointly leave the re-place use case mostly dead.  → FIX: Clarify precedence: is a lifecycle/destroyed event an INPUT event (gated by focus/foreground per LIFE-7) or a lifecycle event (LIFE-7 says 'lifecycle/resize reports may still queue')? LIFE-7 already exempts lifecycle/resize from the focus gate but NOT from the foreground-owner-change cancellation. State explicitly whether destroyed (lifecycle class) survives foreground-owner change so the app can re-place on next focus, or is dropped. Pick one and cite it in both LIFE-3 and LIFE-7.
- [low] LIFE-9: Partial-erase test claim is geometrically suspect. test_note (3) places an inline viewport 'spanning several cells on one row' then does 0J 'from a cursor mid-viewport so only some placeholder cells are erased' and asserts a 'shrunk CellRect'. A CellRect is a rectangle (col,row,cols,rows); erasing the right-hand portion of a single-row run can shrink the rect, but for a multi-row inline viewport a mid-row 0J erases a NON-RECTANGULAR subset of placeholder cells, which cannot be represented as a shrunk CellRect. The decision's 'recomputed CellRect over surviving cells' is ill-defined when survivors are non-rectangular.  → FIX: Define the recompute rule for non-rectangular survivor sets: either (a) the viewport is destroyed once ANY full row/the anchor run is broken (simplest, matches 'cells authoritative'), or (b) CellRect becomes the bounding box of survivors and the now-erased interior cells are treated as holes — but then specify compositing over holes. Recommend (a): destroy if the placeholder run is no longer a contiguous rectangle, since inline placeholder binding (kitty-style) assumes a rectangular cell region. Fix the test to a single-row case or to assert destruction.

**Cross-cluster:**
- F2 MISMATCH WITH REGISTER (important): the binding F2 in this task ('NO RGP COMPATIBILITY = pure clean break, no adapter') CONTRADICTS the register's F2 ('RGP coexistence depth') and several P0 items written under the old F2: RGP-9 ('adapter p = clear-then-set, u = sparse merge'), CAPS-8 ('keep RGP-via-adapter silent... adapter mapping'), and design-doc 4 (Relationship to RGP) + 15.4's 'permissive path_resolver applies to RGP-via-adapter only'. Under binding F2 all 'adapter' wording across SEC/CAPS/RGP clusters is MOOT and must be re-resolved: there is no adapter, so CAPS-8's asymmetry resolution and RGP-9 are dead. The LIFE resolutions correctly treat RGP as untouched legacy, but sibling clusters built on the adapter must be flagged. Note: MEMORY.md says RGP path policy stays permissive in v1 for ecosystem compat — that is RGP's OWN legacy code path, consistent with F2 (TGP has no path=), not a bridge.
- LIFE-6 vs CAPS-1/SEC-1/CAPS-7: LIFE-6's token_denied error and per-token routing depend on (a) token being in the TEXT header (so it survives binary cap/abort, per CAPS-1/SEC-1's txn-in-header decision) and (b) the handshake being the error opt-in (CAPS-6/CAPS-7). The token must be hoisted into the text header alongside txn, and the q/r handshake must mint the token (CAPS-1/CAPS-7). LIFE-6 says 'Edit 7.1 to show the token in the q/r exchange' but does not coordinate with CAPS-1's text-header requirement — these must be unified or token_denied cannot be cited on a binary frame.
- LIFE-5/LIFE-2 vs CAPS-4: LIFE-5 says device loss emits NO error and is invisible. CAPS-4 recommends emitting an unsolicited tgp;r / caps_changed on device loss because VRAM caps can drop after recreation. These conflict: LIFE-5's 'invisible recovery, no signal' vs CAPS-4's 'app must learn caps changed'. Reconcile: device loss may need to emit a caps-changed signal (CAPS-4) even though it emits no per-txn error (LIFE-5). The 'force-all-dirty re-upload' could itself bust a reduced VRAM cap, producing a cap_exceeded that LIFE-5's 'no error' rule forbids — pick a winner.
- LIFE-7/LIFE-5 vs EXPL-6: EXPL-6 already defines focus-out/pointer-leave mid-drag behavior (terminate gesture, emit synthetic ev=up then ev=leave, damping continues to rest). LIFE-5 and LIFE-10 say ONLY device loss may cancel an in-flight drag, and LIFE-7 says input events are suppressed while unfocused. These three give DIFFERENT answers for 'focus lost mid-drag': EXPL-6 ends the gesture and emits up+leave; LIFE-10 says only device loss cancels (so focus loss must NOT cancel); LIFE-7 suppresses emission while unfocused (so the up+leave EXPL-6 wants to emit would be suppressed). This is a three-way cross-cluster contradiction that must be resolved.
- LIFE-8 vs EXPL-1/EXPL-2/EXPL-10: LIFE-8's SHIFT-drag-forces-selection and explore-consumes-drag are consistent with EXPL-1/EXPL-2, but LIFE-8 does not address WHEEL over an inline viewport inside a scrollable pager (EXPL-10's open case: zoom vs text-scroll). A selection/copy resolution that ignores wheel-scroll routing through the same cells is incomplete where it overlaps EXPL-10. Ensure LIFE-8 and EXPL-10 agree on the inline-in-pager pointer model.
- LIFE-3/LIFE-9 destroyed event vs EXPL-3: EXPL-3 defines subscription lifecycle on vp destroy ('vp destroy drops all of that viewport's subs and frees its pick target'). LIFE-3/LIFE-9 destroy viewports via eviction/erase and emit ev=destroyed. Confirm the eviction/erase destroy path runs the SAME sub-teardown + pick-target free as an explicit vp destroy (EXPL-3), and that ev=destroyed is emitted BEFORE the sub is torn down, else the owning app never receives it.

**Missed:**
- NONE of the 11 LIFE resolutions addresses the alt-screen ANIMATION/EXPLORE clock interaction: when a primary-bound inline viewport with auto_spin (LIFE-1 hides it on alt enter), does the injectable animation clock keep advancing while hidden (wasting CPU and emitting ev=camera into the now-foreground alt-screen TUI, the exact mouse-leak class LIFE-7 guards) or pause? LIFE-1 only gates RENDER visibility, not the clock; LIFE-7 only cancels on owner-change, not on alt-screen switch by the SAME app. A same-app vim-on-alt-screen with a hidden primary spinner is unhandled.
- LIFE-2 RIS does not state interaction with LIFE-1 screen affinity / Both-affinity pinned HUDs: RIS exits alt screen first (verified in term.rs ris()) then tears down everything — so a Both-affinity pinned HUD is destroyed by RIS. That is probably correct, but the resolution never says RIS also clears affinity state / the stashed grid; it should explicitly state RIS resets screen_affinity bookkeeping and any stashed-viewport state to avoid a dangling stash after teardown.
- No resolution covers the bounded-resource accounting of the per-viewport OFFSCREEN targets that LIFE-1 says are 'retained, not rendered' while hidden on the inactive buffer. Hidden-but-retained offscreen color+depth+MSAA+pick targets still consume VRAM against the shared max_vram_mb cap (Principle 3, VIEW-10). A TUI that toggles alt-screen with many primary inline viewports keeps all their GPU targets resident while hidden. LIFE-1 explicitly says 'no GPU teardown' on hide — this is a VRAM-pinning vector the safe-by-default principle should bound (e.g. free offscreen targets on hide, keep only CPU state, re-create lazily like LIFE-5's force-reupload).
- DECSTR (soft reset) is asserted to leave the TGP scene UNTOUCHED, but the resolution does not address whether DECSTR resets the input/explore MODE state (raw/explore/events flags per F4) — DECSTR is a 'mode soft reset'. If DECSTR resets mouse_mode (it does in term.rs: ris resets it, soft reset path should be checked), the interaction with active TGP subscriptions/explore is undefined. The boundary between 'pen/mode soft reset' and TGP interaction modes is unstated.
- No resolution defines behavior of CSI 3J / scrollback clear vs the LIFE-3 destroyed-event when MANY inline viewports are evicted at once (a single 3J could evict hundreds). This is a potential reply-buffer flood on the single FIFO back-channel (EXPL-11 backpressure concern): hundreds of ev=destroyed frames queued at once. Should destroyed events be coalesced/batched, or is per-viewport emission acceptable? Unaddressed and interacts with the shared queue_reply path.
- LIFE-11 mentions an 'in-progress binary frame' abort on EOF, but no LIFE resolution addresses a binary frame in progress when RIS arrives (LIFE-2 lists 'in-progress binary frame' in the teardown list, good) — however RIS is delivered AS BYTES on the same stream that is mid-binary-read (consume-exactly-N state). Per CAPS-5, an ESC c that lands inside a len=N binary read is consumed as raw payload, NOT interpreted as RIS. So 'RIS tears down in-progress binary frame' is unreachable while that very frame is consuming the RIS bytes. This framing-state deadlock between LIFE-2 and the binary-read state (WIRE/CAPS-5) is missed.

## VIEW  (consistency_ok=False)

**Issues:**
- [high] VIEW-2: The rejection is specified as `tgp;x;txn=<n>;code=cap_exceeded` but a `vp` frame carries NO txn/op. The design doc (§18.1 line 729, taxonomy line 351) shows `tgp;vp;id=2;...` with no txn field, and §15.3 errors are explicitly 'per-txn and cite the offending op index' — a structure that does not exist for a standalone vp frame. The resolution invents a txn=<n> on a vp error without defining where that correlation id comes from for vp frames. Same defect in the test_note which asserts a `tgp;x;...;code=cap_exceeded;detail=max_inline_cols` without saying what correlates it to the rejected vp.  → FIX: Define the correlation id for vp-frame errors before citing it. Either (a) add a `txn=`/`id=` to the `vp` control frame in §10.1/§8.5 and have the error echo it (preferred — aligns with SEC-1's 'hoist txn into the header so cap-rejections cite it'), or (b) specify the vp error echoes `vp=<id>` (the requested viewport id) instead of txn. Update the test_note to assert that correlation field. Pin this jointly with SEC-1/WIRE-2 so all reject paths cite a stable id.
- [high] VIEW-5: Invents a 'viewport diagnostic channel' / 'viewport diagnostics' subscription that does not exist anywhere in the design. §12 `sub` selects a viewport plus a node list plus input `ev` types (click/hover/enter/leave/drag/wheel/camera/resize per §12.5); there is no diagnostic/error subscription concept. The resolution gates `tgp;x;vp=<id>;code=camera_missing` on 'subscribed to a viewport diagnostic channel' — an undefined opt-in. Also the emitted error shape `tgp;x;vp=<id>;code=camera_missing;detail=<name>` has no txn and is edge-triggered per state-transition, which is a wholly new tgp;x variant inconsistent with §15.3's per-txn/op model (and with CAPS-7's 'a completed q/r handshake IS the error-reporting opt-in', meaning error opt-in is the handshake, not a per-viewport channel).  → FIX: Either drop the per-viewport diagnostic and rely on the handshake-level error opt-in (CAPS-7) emitting a one-shot non-txn diagnostic `tgp;x;vp=<id>;code=camera_missing;detail=<name>`, OR define the diagnostic channel as a concrete new opt-in (a `sub` flag like `diag=1` or a `vp` setting) in §12/§10.1 with a stated wire grammar. State explicitly that this tgp;x is a non-txn, edge-triggered diagnostic (a documented exception to the per-txn rule) so it does not contradict §15.3. Cite F1: route to owning token only.
- [high] VIEW-6: VIEW-6 makes `camera` optional and says explore on a camera-less viewport auto-promotes the implicit default into a 'terminal-owned Camera node bound to that viewport ... addressable only by the owning token (F1)'. But F1 isolation is about TOKEN-scoped addressing of app-created ids; a terminal-synthesized node living in the shared scene needs a defined id and namespace slot. §8.4 currently says 'one global namespace per session' (directly contradicted by F1 and not yet reconciled in any VIEW resolution). The resolution asserts the synthesized camera 'is not a duplicate app node id' without specifying how the terminal guarantees non-collision against app-chosen string ids in the (per-token) namespace, nor whether it consumes the node cap (§15.2 max nodes, a SHARED cap under F1).  → FIX: Specify the synthesized camera's identity precisely: it must live in a reserved id space the app cannot author (mirror SCEN-8's 'reserved adapter prefix' mechanism — reserve a terminal prefix for synthesized cameras), state it is keyed within the owning token's namespace, and state whether it counts against the shared max-nodes cap. Add a cross-reference that §8.4's 'one global namespace' is superseded by F1 per-token scoping (this is currently unreconciled and should be flagged to the SCEN/LIFE resolver).
- [high] VIEW-8: VIEW-8 defines the COMPOSITE order tuple as (z, owning-token-order, ViewportId) but says nothing about INPUT hit-testing order, even though EXPL-8 is an open finding that input must hit-test top-down 'by z among viewports with an active sub' and fall through transparent (clear=None) viewports. If VIEW-8 picks composite tiebreak (z, token-order, ViewportId) but EXPL resolves input tiebreak differently, a click can route to a different viewport than the one visually on top — a silent divergence. VIEW-8 explicitly 'interacts: ... input' in the source finding yet the resolution is silent on it.  → FIX: Add a normative cross-reference that input hit-testing uses the SAME total order tuple (z, owning-token-order, ViewportId) as compositing, with EXPL-8's transparent fall-through layered on top (top-down by that tuple among subscribed viewports; pierce empty clear=None pixels). Flag to the EXPL resolver that EXPL-8 MUST adopt this exact tuple. Also note: F1 cross-token input routing — a token-B viewport on top of a token-A viewport must not silently capture token-A's pointer; clarify whether the top viewport by z captures regardless of token, which has F4 SGR-suppression implications across tokens.
- [medium] VIEW-1: Two internal gaps. (1) The `destroyed` lifecycle event is `tgp;e;vp=<id>;ev=destroyed;reason=evicted`, but §12.5 says `e` reports are 'only emitted for viewports/nodes the app subscribed to (sub)'. A `sub` in §12.5 subscribes to INPUT events (click/hover/etc.); the resolution adds `destroyed` to the `ev` enum but does not state whether an input sub (e.g. ev=click only) implicitly also delivers lifecycle events, or whether the app must subscribe to ev=destroyed explicitly. As written 'if the owning token's app has an active sub on that viewport' is ambiguous about sub granularity (§12.5 says sub selects which ev types). (2) 'treated as not-rendered for the frame in which the last cell leaves the visible+scrollback set, then destroyed' — sequencing of the not-rendered frame vs destroy vs event emission is hand-wavy and not asserted by the test_note (the test only checks final membership, not the one-frame transition).  → FIX: State explicitly that lifecycle events (destroyed) are delivered to any active sub on the viewport regardless of which input ev types were selected (or add `lifecycle`/`destroyed` as a selectable ev category in §12.5). Tighten the not-rendered-then-destroyed sequencing into a deterministic rule (e.g. 'destroyed in the same advance() that evicts the last cell; emitted exactly once') and add a test_note assertion on the transition ordering, not just final state.
- [medium] VIEW-3: DETACH-ON-OVERWRITE auto-destroys the viewport when the live placeholder set drops below 'required cell count' and emits reason=detached. But the test_note's mechanism for VIEW-3/VIEW-12 ('write a normal glyph X into a placeholder cell') overlaps with VIEW-12's re-anchor path and is not clearly distinguished from legitimate re-emit. More importantly, the rule 'Re-emitting the SAME placeholder glyph into a cell is not an overwrite' assumes the terminal can distinguish a placeholder glyph from ordinary text at cell-write time — this requires a defined placeholder-cell tagging scheme (kitty uses U+10EEEE + diacritics) that no VIEW resolution actually specifies. Without pinning HOW a placeholder cell is identified on the wire/grid, 'detach on non-placeholder glyph' and 'same placeholder glyph is not an overwrite' are not implementable or testable.  → FIX: Add (or cross-reference) a normative definition of the placeholder-cell encoding (the Unicode placeholder codepoint + per-cell viewport-id/row/col association) that §10.2 currently only gestures at ('Unicode-placeholder-style cell binding à la kitty'). The detach/re-emit rule depends entirely on this. Then the test_note can assert the live-set membership against a concrete cell-tag accessor.
- [medium] VIEW-5: Default-camera auto-framing 'computed each frame from the bounding sphere of the viewport's root subtree (or whole scene if root=None)' has no defined behavior for the EMPTY case (root subtree has zero renderable geometry, e.g. only a Camera or only Light nodes, or scene empty). An auto-frame of an empty/degenerate bounds is undefined (NaN look-at, zero-radius sphere). The test_note never exercises the empty-scene case.  → FIX: Define the degenerate-bounds fallback (e.g. a fixed identity/default pose looking down -Z at the origin with default FOV when the bounding sphere is empty or zero-radius), and add a PTY-bytes-in test: handshake + viewport with no camera and no renderable geometry → assert the effective camera/view matrix is the defined finite default (no NaN, no panic).
- [medium] VIEW-10: Same txn/correlation defect as VIEW-2: `tgp;x;code=cap_exceeded;detail=max_viewport_px` is emitted for a rejected `vp` frame that carries no txn (see §18.1 line 729/751). The fix must be consistent with VIEW-2's fix. Additionally, the OVERSIZED rule says reject when 'on-screen clamped size still exceeds the per-viewport pixel cap' — but a viewport can be created on-screen at <=cap, then a RESIZE/reflow (VIEW-4) could grow its on-screen footprint past the cap. The resolution does not say whether a reflow that pushes an existing viewport over max_viewport_px rejects, clamps, or auto-destroys; this is the dynamic case VIEW-4 introduces.  → FIX: Unify the vp-error correlation field with VIEW-2. Add a rule for the dynamic over-cap case (reflow/resize growing an existing viewport past max_viewport_px): recommend it is clamped-to-cap-on-screen (not rejected, since there is no txn to reject and the viewport already exists) and a one-shot diagnostic is emitted to the owning token — and make this consistent with VIEW-4 which currently only mentions clipping to the on-screen intersection, not the pixel cap.
- [low] VIEW-7: The cache-recomposite invariant is sound given grounding (text at constant z=0.5, LessEqual — verified in toastty-render/src/lib.rs:253-254). However VIEW-7 asserts re-composite validity 'iff (a) content/camera/size dirty flags are clear AND (b) the move is a screen-space translation' but does not address INTER-VIEWPORT z-order interaction: if another overlapping viewport (VIEW-8) is re-rendered or changes z while this viewport only translated, the cached layer of THIS viewport is still valid but the composite ORDER changed. The resolution treats the cache as if viewports composite independently; it should state the cached-layer validity is per-viewport and orthogonal to the global composite-order recompute (which VIEW-8 says happens every frame anyway).  → FIX: Add one sentence: cached-layer validity is per-viewport and independent of the global composite order; the compositor re-sorts every frame (VIEW-8) regardless of which layers are cached, so a neighbor's z change or re-render never invalidates this viewport's cached layer. This closes the only remaining ambiguity and is cheap.
- [low] VIEW-9: VIEW-9 says the offscreen depth buffer 'is cleared to FAR so un-drawn pixels never spuriously occlude text'. Grounding confirms the existing pass is a single shared Depth32Float with LessEqual and text at z=0.5. But VIEW-9 (per-viewport offscreen color+depth target) presupposes per-viewport offscreen targets, while the GROUNDING explicitly states 'Today: single shared Depth32Float pass ... no per-viewport offscreen targets, no MSAA, no instancing'. VIEW-9/VIEW-7/VIEW-1 all assume per-viewport offscreen color+depth+pick targets that do not yet exist. This is fine as a clean-break design target, but no VIEW resolution flags that this is net-new renderer architecture (not a generalization of current code), which affects the implementation-plan estimate and the testability claim (CPU-side target-spec accessors must be built from scratch).  → FIX: Add a grounding-honesty note (in VIEW-9 or a cluster-level note) that per-viewport offscreen color/depth/pick targets are NEW vs the current single-shared-pass renderer, so the CPU-side target-spec/blend-state accessors the test_notes rely on must be introduced. This is a missed-grounding flag, not a logic error.
- [low] VIEW-4: The split-across-wrap case renders 'the offscreen layer once at the viewport's logical (cols x rows) size and composites it into the SMALLEST axis-aligned grid rect that contains all live placeholder cells, clipping per VIEW-10'. Compositing a logical-size layer into a possibly-larger bounding rect (when cells split across two rows, the bounding rect spans both rows and intervening cells that are NOT part of the viewport) means the layer is composited over cells it does not own — including unrelated text between the split halves. The resolution does not define what happens to the non-placeholder cells inside that bounding rect (are they occluded? composited-over? left alone?). This directly interacts with VIEW-3 occupancy.  → FIX: Define the split-wrap composite precisely: composite only into the cells that are live placeholders (not the full bounding rect), OR explicitly accept that the bounding-rect approach occludes intervening cells and state the occupancy rule for them (consistent with VIEW-3 clear=Some/None). A per-cell composite mask is the safer rule and is CPU-testable (assert which cells are flagged composited).

**Cross-cluster:**
- F1 vs §8.4 'one global namespace per session' is UNRECONCILED. Every VIEW resolution assumes token-scoped addressing and event routing already exists (VIEW-1/2/5/6/8/10 all cite 'routed to owning token'), but §8.4 still declares a single global namespace and no VIEW resolution flags that §8.4 itself must be rewritten for F1. The SCEN/LIFE/CAPS resolver owning F1 (LIFE-6/CAPS-10/SCEN-8) must rewrite §8.4 addressing; VIEW resolutions depend on that rewrite landing. Flag a hard dependency.
- EXPL-8 (input hit-test order for overlapping viewports) MUST adopt VIEW-8's exact composite tuple (z, owning-token-order, ViewportId) plus transparent fall-through. If the EXPL resolver picks a different input tiebreak, click routing diverges from the visual top viewport. This is a two-way binding consistency requirement.
- EXPL-1/EXPL-2 (F4) interact with VIEW-3 and VIEW-8: F4 says an active sub captures pointer events within the viewport CELL RECT and suppresses SGR there. But under VIEW-3 detach-on-overwrite and VIEW-1 eviction, the cell rect (live placeholder set) shrinks dynamically. The EXPL/F4 resolver must define capture against the CURRENT live placeholder cells (VIEW-12 authority), not the stored {line,col} or original cells. Cross-check that F4 capture uses the same cell-authority as VIEW-12.
- VIEW-5/VIEW-6 synthesized/default camera + EXPL-7 camera sync-back: EXPL-7 reports ev=camera as the camera node's TRS. For a camera-less viewport using explore (VIEW-6 auto-materializes a terminal-owned camera), EXPL-7 sync-back must report THAT synthesized camera's TRS — confirm the EXPL resolver handles sync-back for terminal-synthesized cameras (and that the app cannot node.upsert it back across token isolation).
- SCEN-8 reserved-id-prefix mechanism is the natural home for VIEW-6's terminal-synthesized camera id non-collision guarantee. VIEW-6 asserts non-collision but should explicitly reuse SCEN-8's reserved-prefix rule rather than inventing a separate scheme. Flag to the SCEN resolver.
- LIFE-1 (alt-screen enter/leave binds viewports to screen buffer of creation; inline hidden on switch) interacts with VIEW-1 eviction: an inline viewport hidden on alt-screen entry must NOT be eviction-destroyed merely for being not-rendered. VIEW-1 keys destroy on placeholder-cell eviction from the scrollback ring, which is independent of alt-screen hide — confirm LIFE-1 'hidden' state does not trip VIEW-1's 'zero live placeholder cells = not-rendered then destroyed' path (hidden != evicted). This is a subtle two-cluster consistency check.
- WIRE-2/SEC-1 (parser back-channel + hoist txn into the text header) is the foundation for VIEW-2/VIEW-10 cap_exceeded errors on vp frames. Those VIEW resolutions cite txn=<n> on a vp error but vp frames have no txn today (§18.1). SEC-1's 'hoist txn into the text header so cap-rejections cite it' must extend to vp frames, or VIEW errors must echo vp=<id> instead. Cross-pin required.

**Missed:**
- Placeholder-cell ENCODING is never pinned by any VIEW resolution, yet VIEW-1/3/4/12 all depend on identifying which grid cells are a viewport's live placeholders and distinguishing 're-emit identical placeholder' from 'overwrite with text'. §10.2 only says 'Unicode-placeholder-style cell binding (à la kitty)'. Without a normative codepoint + per-cell viewport-id/row/col association scheme, the detach/eviction/reflow rules are not implementable or unit-testable. This is the single biggest missed item — it underpins the entire F3 cluster.
- No VIEW resolution addresses the SHARED resource caps vs PER-TOKEN isolation tension that F1 explicitly raises ('resource caps are shared but addressing and event routing are isolated'). VIEW-6 silently creates a terminal-owned node (consumes shared max-nodes cap), VIEW-1/9/10 allocate shared VRAM for offscreen+depth+pick targets. None states how per-viewport offscreen-target VRAM counts against the SHARED max_vram_mb cap, nor what happens when a token's viewport allocation would exceed the shared cap that other tokens also draw from (a cross-token DoS: token A exhausts shared VRAM, token B's viewports fail). This needs a fairness/accounting rule, especially given F1's shared-caps clause.
- The cluster never addresses CELL→PIXEL ROUNDING (the source finding VIEW-10 lists 'cell->pixel rounding' as an interaction). When a cell rect maps to a fractional pixel rect (non-integer cell metrics, fractional scaling), the on-screen-clamp (VIEW-10), bounding-rect composite (VIEW-4), and cached-layer translation (VIEW-7) all need a defined rounding rule for determinism. The grounding cites pipeline.rs:196-212 cell-metrics mapping; a deterministic rounding rule (floor/round/ceil) should be pinned so CPU-side pixel-rect assertions are exact.
- RenderOpts.msaa per-viewport (§11.4) interacts with VIEW-9's premultiplied-alpha offscreen + VIEW-7's cached depth, but no VIEW resolution states how MSAA resolve interacts with the cached color+depth layer (is the cached layer pre- or post-resolve? does resolved depth even exist for re-composite?). §11.1 does 'msaa resolve' before composite; VIEW-7 caches 'color+depth' — resolved MSAA depth is ill-defined. Given grounding says no MSAA today, this is net-new and unspecified.
- Pinned viewports get almost no coverage. Every resolution is inline-centric (F3-driven). Pinned viewports have a {col,row} screen anchor (§10.1) and are bounded by VIEW-10's pixel cap (VIEW-2 explicitly excludes them from inline cell caps). But pinned interaction with DECSTBM/clip (VIEW-11), z-order vs inline (VIEW-8 covers), eviction (N/A, no scrollback line), and alt-screen (LIFE-1) is thin. At minimum VIEW should state pinned viewports are NOT subject to placeholder-cell authority (VIEW-12) or eviction (VIEW-1) — currently only implied.
- No resolution defines behavior when max_inline_cols/max_inline_rows (VIEW-2) or max_viewport_px (VIEW-10) caps would be EXCEEDED by terminal-driven reflow of an already-created viewport (VIEW-4 grows it). VIEW-2 only checks at creation ('on a tgp;vp ... REJECTS'); a font-size-decrease or width-increase reflow can push an in-bounds inline viewport over the cell cap or pixel cap post-creation. The dynamic-recheck-on-reflow rule is missing across VIEW-2/4/10.

## SEC  (consistency_ok=False)

**Issues:**
- [high] CAPS-1/CAPS-2: Direct internal contradiction on whether consume-N runs for an over-cap len. CAPS-1 says on a len-over-cap rejection the terminal '(1) still transitions into consume-exactly-N mode and drains/discards exactly N bytes (or to the negotiated stall limit)' to keep framing aligned. CAPS-2 asserts the opposite as the load-bearing premise of its anti-drip-feed argument: 'a too-large len is already rejected at header parse (CAPS-1), so consume-N only ever runs for an under-cap len.' Both cannot hold. If over-cap DOES drain N (CAPS-1), then consume-N runs for an arbitrarily large attacker-chosen N and CAPS-2's bounded-progress proof collapses; if over-cap does NOT drain (CAPS-2), then CAPS-1's resync mechanism is undefined and the announced N raw bytes leak into the text grid (the WIRE-3/WIRE-8 desync vector).  → FIX: Pick one model and state it in both. Recommended (matches WIRE-8): over-cap len DOES enter a bounded discard state but capped at an absolute hard ceiling (not 'the negotiated stall limit', which is never defined). Define the ceiling as an advertised cap (e.g. max_drain_bytes_session) above which the parser declares unrecoverable desync, returns to Ground, and emits a distinct code. Then CAPS-2's 'consume-N only runs for under-cap' must be reworded to 'the COMMIT/reassembly consume-N path only runs for under-cap; the DISCARD-and-resync path is separately bounded by the hard drain ceiling.'
- [medium] CAPS-1/CAPS-2: Both cite 'the negotiated stall limit' / 'an absolute hard ceiling' but neither defines it as a concrete advertised cap, and CAPS-9 (the unified closed cap set) does NOT list any drain/stall ceiling. So the one bound that actually prevents the over-cap-len DoS is left vague and is absent from the advertised vocabulary, violating CAPS-9's own rule 'the terminal MUST NOT enforce a cap it did not advertise' and principle 4 (app owns fallback).  → FIX: Add the drain ceiling and the CAPS-2 inactivity bound K to CAPS-9's closed advertised set (e.g. max_drain_bytes_session, max_partial_frame_idle_reads). Cross-reference from CAPS-1/CAPS-2.
- [medium] CAPS-2: test_note (b) asserts 'pending budget accessor reads 0' after RIS abort, and (a) asserts parser back in Ground, but neither test asserts the deterministic inactivity bound K via the injectable clock seam the decision relies on — test (a) says 'call advance with empty/unrelated input N>K times' which is hand-wavy about what advancing the advance-call counter looks like through Parser::advance and never asserts the counter value or that K is the injected/configured value. The grounded clock today is RGP's real-Instant tick (scene.rs:198 tick_animations(now: Instant)); the decision invents an 'advance-call count' clock that does not yet exist and the test does not assert it is wired to an injectable seam.  → FIX: Add an assertion that drives the abort by an EXPLICIT injected counter/clock value (e.g. set K=3, call advance 3 times with no bytes for the frame, assert truncated fires on exactly the 3rd) and assert a &self accessor exposing the pending-frame idle-read count, so the determinism claim is actually tested through the PTY seam rather than asserted in prose.
- [medium] CAPS-1: CAPS-1 mandates 'txn and len mandatory' and makes txn a u32 in the binary header, but does NOT address WIRE-7's ordering requirement (enc= must precede len= so the parser knows raw-byte mode vs base64-scan mode before acting on len). Since the parser must enter consume-N at TEXT-header parse time based on enc=bin, and the doc example (line 312) writes enc before len but never normatively requires that order, CAPS-1's 'validate len at header-parse time, before entering consume-N' is unimplementable when enc appears after len.  → FIX: Add to CAPS-1 doc_changes: enc= MUST precede len= in the binary header (or default enc=b64 when absent), so the framer knows whether len means raw-bytes-to-follow (enc=bin) before it acts. Cross-reference WIRE-7. This is a precondition for the whole CAPS-1/CAPS-2 consume-N machinery.
- [medium] CAPS-3: CAPS-3 says an asset.add arriving for an id with a pending chunked upload 'follows §8.4s collision rule', but §8.4 (doc line 342-343) only defines collision for a COMMITTED immutable asset id (add-on-existing = error/dup_id). The in-flight-chunked-id case is a genuinely separate ambiguity that SCEN-9 raises explicitly (immutability vs in-flight reassembly buffer). CAPS-3 hand-waves it to a rule that does not actually cover the case, leaving it unresolved.  → FIX: Either resolve it here (recommended: an asset.add for an id with an in-flight chunked upload is NOT a dup_id; it depends on the SCEN-9 resolution of begin/chunk/end vs add+more) or explicitly defer to SCEN-9 by id and stop calling it 'the §8.4 collision rule'. Note the dependency in cross_cluster_notes.
- [medium] CAPS-4: CAPS-4 computes net VRAM delta within a txn including 'minus bytes freed by node.remove/asset replacement within the same txn', and test (b) asserts a remove+add of equal size commits at-cap (net ~0). But this requires intra-txn ordering semantics (the remove must be accounted before the add) AND interacts with SCEN-1's cascade-delete rule (node.remove freeing a subtree frees referenced assets only if no other node references them — CAPS-4 itself says residency is per-asset, counted once). If the removed node was the LAST referencer the asset frees; if not, net delta is wrong. CAPS-4 does not state that the per-asset refcount is evaluated post-removal within the txn.  → FIX: State that VRAM delta is computed against the txn's FINAL post-apply asset reference set (refcount each asset after all adds/removes/upserts in the txn are notionally applied), not op-by-op, so a remove that drops the last reference frees and a remove of a still-referenced asset frees nothing. Cross-reference SCEN-1 (cascade delete) and the per-asset-once counting rule.
- [medium] CAPS-9: CAPS-9 renames EVERY advertised cap field (max_verts -> max_verts_per_asset, max_vram_mb -> max_vram_mb_session, max_instances -> max_instances_per_buffer, etc.). The current §7.1 doc example (line 266) ships max_verts/max_instances/max_vram_mb/max_msg_mb with no suffixes. This is fine for a clean break (no RGP compat to preserve, F2), BUT CAPS-9 keeps max_msg_mb un-suffixed 'as the message-size cap' while suffixing everything else — an inconsistency in its own self-describing-scope principle (max_msg_mb is per-message/per-frame and should arguably be max_msg_mb_message or noted). More importantly, CAPS-5's detail= values and CAPS-7's detail= values (detail=max_verts, detail=max_texels) use the OLD un-suffixed names, while CAPS-9 test (b) asserts 'the detail= string equals the advertised field name'. So CAPS-5/CAPS-7 detail strings will NOT equal the CAPS-9 advertised names — a cross-finding vocabulary mismatch.  → FIX: Make CAPS-5 and CAPS-7 detail= enums use the EXACT suffixed names from CAPS-9 (detail=max_verts_per_asset, detail=max_texels_per_texture, etc.), or explicitly state detail= uses the un-suffixed short form and drop CAPS-9 test (b)'s 'detail equals advertised field name' assertion. As written they contradict.
- [low] CAPS-6: CAPS-6 says a raw tgp;p from an un-queried (untrusted) writer is 'processed for safety (caps still enforced, scene still protected) but its errors are NOT emitted ... dropped silently like a dumb reader.' This is a security-relevant decision that lets untrusted PTY bytes MUTATE the shared scene (commit nodes/assets) with no token and no proven listener — it only withholds the error reply, not the mutation. Under F1 (tokenless/untrusted writers cannot address or retarget nodes owned by a different token) this is consistent for ADDRESSING existing nodes, but CAPS-6 does not say whether a tokenless writer may CREATE new (default-namespace) nodes. LIFE-3 raises exactly this cross-process scene-pollution/DoS. Left ambiguous here.  → FIX: State explicitly whether a tokenless tgp;p may create/mutate default-namespace nodes at all, or whether scene mutation (not just error emission) requires a prior valid tgp;q. Recommended: tokenless writers may be processed for safety but their mutations land only in the default/global namespace they own (per CAPS-6's own 'default/global namespace it owns' wording for queries) — make that the same for raw patches. Cross-reference LIFE-3 and F1.
- [low] CAPS-10: CAPS-10 coalescing ('multiple node.instances for the same node within a single render frame collapse to the latest; one upload per node per frame, bumping scene_revision once') is asserted as testable, but the grounded dirty model bumps scene_revision PER MUTATION (term.rs / SCEN flags scene_revision per mutation), and there is no 'render frame boundary' concept in the GPU-free Parser::advance(&mut Term) seam — frames are a renderer concept, not a parser/Term-state concept. test (b) asserts 'scene_revision advanced by exactly one' across three patches, which is unobservable at the Parser->Term seam without a defined frame-boundary tick on Term. The coalescing rule conflates renderer-frame with txn-commit.  → FIX: Define coalescing at a CPU-observable boundary: either (a) coalesce within a single txn (multiple node.instances ops for the same node in one patch collapse to the last — fully testable via one Parser::advance) and let the renderer's asset_revision/scene_revision dirty-split (principle 6) naturally collapse cross-txn redundant uploads, or (b) add an explicit Term::begin_frame()/composite tick accessor the test can call. As written, 'one upload per node per frame, scene_revision +1' is not assertable through the mandated PTY-to-internal-state seam.
- [low] CAPS-5: CAPS-5's closed enum omits a code for the CAPS-1/CAPS-2 hard-drain-ceiling / unrecoverable-desync case and for the WIRE-3 len-mismatch (len disagrees with actual CBOR length) case. WIRE-3 recommends emitting parse_error on decode-too-short/too-long, which CAPS-5 could cover under parse_error, but the desync-abort (drain ceiling exceeded) has no code. Since CAPS-5 claims the enum is 'the union of every reject path surfaced across CAPS-1..4, CAPS-7, and F1', a reject path exists that the enum does not name.  → FIX: Add a desync/unrecoverable code (e.g. stream_desync or fold into truncated) for the drain-ceiling-exceeded path from the CAPS-1/CAPS-2 fix, and explicitly map WIRE-3 len/CBOR mismatch to parse_error in CAPS-5's enum notes.

**Cross-cluster:**
- WIRE cluster is a hard dependency for SEC and is not cited. WIRE-2 recommends the exact apc_end -> Option<BinaryFollow{len,enc}> back-channel that CAPS-1 relies on (grounding confirms apc_start/chunk/end currently return unit in parser.rs and apc_end operates on a fully-buffered apc_buffer via mem::take, term.rs:3392 — there is NO consume-N state today, so CAPS-1/CAPS-2 require WIRE-2 to land first). WIRE-3 (len is authoritative, decode mismatch -> parse_error), WIRE-7 (enc must precede len), WIRE-8 (drain-and-discard N on over-cap with a hard ceiling), and WIRE-9 (persist remaining-byte counter + partial buffer across advance calls) are all preconditions of CAPS-1/CAPS-2 and must resolve consistently. Flag: CAPS-1/CAPS-2 must not contradict WIRE-8's hard-ceiling.
- LIFE-3 is the ORIGIN of F1's per-app namespace token (its rec: 'require an opt-in app-namespace token before honoring event subscriptions'). CAPS-6 (token-scoped x/a/e routing), CAPS-5 (bad_token code), and the 'token-tagged reply accessor' test all DEPEND on LIFE-3/SCEN resolving the token negotiation mechanism (where the token is declared — on tgp;q? a separate handshake?). SEC assumes the token exists but never says how it is negotiated. The design doc still says 'one global namespace per session' (line 340) — F1 overturns this and LIFE/SCEN must rewrite §8.4 accordingly.
- SCEN-8 (node-id charset/length + adapter-synthesized id collision) is partly MOOTED by F2 (no adapter ids) but its non-adapter half (id charset/length pinning) still matters for CAPS-5 bad_ref/dup_id and CAPS-7 hardened parsing. SCEN-9 (asset immutability vs in-flight chunked reassembly) directly governs CAPS-3's unresolved in-flight-id-collision case — CAPS-3 must defer to SCEN-9, not to §8.4.
- SCEN-1 (node.remove cascade-delete of subtree) interacts with CAPS-4's intra-txn VRAM delta accounting: freeing a removed subtree only frees referenced assets when the last referencer is removed (CAPS-4 says residency is per-asset, once). CAPS-4 must use the txn's final post-apply asset refcount, consistent with whatever SCEN-1 decides for cascade vs reparent.
- EXPL-2 (F4) defines the sub pointer-capture mechanism that CAPS-6's e-event token routing rides on, and EXPL-3 (sub teardown on node.remove/vp destroy) plus LIFE-4 (cancel subs/explore/anim on subscribing-app exit / foreground-process change) determine WHEN a token's e/x/a routing stops. CAPS-6's 'route only to owning token' is consistent with these but assumes the per-token reply demultiplexing infrastructure that EXPL/LIFE must build (today queue_reply -> single shared pty_replies FIFO, term.rs:3953 — there is no per-token reply stream; the 'token-tagged reply accessor' in CAPS-6 test (d) does not exist yet).
- RGP cluster: F2 moots every RGP-reconciliation finding. CAPS-8 correctly self-MOOTs and CAPS-11 correctly drops the differential RGP-vs-native fuzz target. CAPS-8's doc_changes to delete §4 and adapter rows in §1/§1.2/§3.1/§5/§19 are warranted — verified the design doc is still saturated with adapter language (lines 18, 24, 91, 119: 'RGP adapter so existing ratty;g; apps keep working', 'r->asset.add; p->node.upsert'). Whoever resolves RGP cluster must also strip these. The legacy RGP path (term.rs:3413-3419, real-Instant ticks at scene.rs:198) stays untouched per F2/MEMORY.
- CAPS-3's cumulative-check-before-accept + immediate-free model is verified to already exist in the RGP handler (handler.rs:189-228: caps checked before accepting each chunk, drop_pending frees, pending_bytes()/pending_count() accessors, per_id_cap + total_cap). F2 forbids reusing that code path; CAPS-3 correctly restates it as a TGP-native rule. But note the RGP path DROPS SILENTLY on overflow (no reply) whereas CAPS-3 emits cap_exceeded — this is genuinely new behavior gated on CAPS-6's listener-proven opt-in, so a tokenless writer's over-cap chunk is still silently dropped (consistent, but worth stating explicitly in CAPS-3).

**Missed:**
- No resolution addresses what happens to in-flight binary/chunk state and the pending budget on a NAMESPACE-TOKEN level: under F1 caps are SHARED but addressing is isolated. CAPS-2/CAPS-3 say pending budget is 'per-session' but F1 makes namespaces the unit of isolation. Is the pending budget per-token or per-session-shared? A malicious token could exhaust the shared per-session pending budget and DoS another token's legitimate chunked upload (cross-token resource starvation). CAPS-3 says 'per-session total pending cap' — under F1 multi-token this is a shared-resource DoS surface that no SEC resolution flags. This is exactly the principle-3 safe-by-default threat for the multi-writer case LIFE-3 raised.
- CAPS-1/CAPS-5 mandate txn is a u32 in the binary header, but the design doc (line 380) says 'txn is an app-chosen correlation id' with no type, and the §8.6 CBOR example (line 362) shows txn:42 INSIDE the CBOR body too. No resolution reconciles the header txn (u32, framing-level) vs the CBOR-body txn (correlation, app-level) — are they required to match? If they can disagree, an error citing the header txn may not match the app's body-tracked txn. Should be pinned: header txn is authoritative and the CBOR body MUST NOT carry a conflicting txn (or omit it).
- CAPS-11 fuzz invariant says 'pending-bytes==0 when no upload is in flight (no leak)' and 'vram-used matches the committed scene', but there is no accessor named for the CONSUME-N partial-binary-frame buffer separate from the chunk-reassembly buffer — CAPS-2/CAPS-3 share 'one accountant' (the pending budget) but a fuzz oracle also needs to assert the consume-N remaining-byte counter is 0 at quiescence and that the parser ApcState/binary-read state returned to Ground. The grounded ApcState (parser.rs:5-17) has no binary-read variant yet; CAPS-11's oracle should assert parser state == Ground at quiescence, not only pending_bytes==0.
- No resolution covers the base64 (enc=b64) decode path for the expansion-bomb cap (CAPS-7) or the len cap (CAPS-1): a base64 payload expands ~4:3 and the b64 len is the encoded-char count (WIRE-7), so max_msg_mb and CAPS-7 expanded-output checks must account for the decode expansion of b64 BEFORE the CBOR/glTF expansion. CAPS-7 lists glTF/CBOR/texture loci but omits the base64->bytes expansion as a locus. A b64 frame just under the encoded-len cap still decodes to a larger byte blob fed to CBOR.
- CAPS-4 counts 'instance buffer footprint once per node' and CAPS-10 governs node.instances churn, but neither addresses the renderer grounding that today there is a SINGLE shared Depth32Float pass with no per-viewport offscreen targets, no MSAA, no instancing (grounding). VRAM accounting for instancing (CAPS-4) and the dirty-split coalescing (CAPS-10, principle 6) assume an instancing path and per-asset GPU residency that does not exist yet — these are net-new renderer subsystems. Not a blocker for the wire/parser rules, but the resolutions should note the GPU-side accounting is purely CPU-known-footprint bookkeeping (CAPS-4 says this) with no actual GPU residency to reconcile in v1, which is consistent but means CAPS-10's 'VRAM churn' framing is aspirational until instancing lands.
